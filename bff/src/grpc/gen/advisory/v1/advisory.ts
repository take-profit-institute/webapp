// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: advisory/v1/advisory.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "candle.advisory.v1";

export enum RiskTolerance {
  RISK_TOLERANCE_UNSPECIFIED = 0,
  RISK_TOLERANCE_CONSERVATIVE = 1,
  RISK_TOLERANCE_MODERATE = 2,
  RISK_TOLERANCE_AGGRESSIVE = 3,
  UNRECOGNIZED = -1,
}

export function riskToleranceFromJSON(object: any): RiskTolerance {
  switch (object) {
    case 0:
    case "RISK_TOLERANCE_UNSPECIFIED":
      return RiskTolerance.RISK_TOLERANCE_UNSPECIFIED;
    case 1:
    case "RISK_TOLERANCE_CONSERVATIVE":
      return RiskTolerance.RISK_TOLERANCE_CONSERVATIVE;
    case 2:
    case "RISK_TOLERANCE_MODERATE":
      return RiskTolerance.RISK_TOLERANCE_MODERATE;
    case 3:
    case "RISK_TOLERANCE_AGGRESSIVE":
      return RiskTolerance.RISK_TOLERANCE_AGGRESSIVE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RiskTolerance.UNRECOGNIZED;
  }
}

export function riskToleranceToJSON(object: RiskTolerance): string {
  switch (object) {
    case RiskTolerance.RISK_TOLERANCE_UNSPECIFIED:
      return "RISK_TOLERANCE_UNSPECIFIED";
    case RiskTolerance.RISK_TOLERANCE_CONSERVATIVE:
      return "RISK_TOLERANCE_CONSERVATIVE";
    case RiskTolerance.RISK_TOLERANCE_MODERATE:
      return "RISK_TOLERANCE_MODERATE";
    case RiskTolerance.RISK_TOLERANCE_AGGRESSIVE:
      return "RISK_TOLERANCE_AGGRESSIVE";
    case RiskTolerance.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum InvestmentHorizon {
  INVESTMENT_HORIZON_UNSPECIFIED = 0,
  INVESTMENT_HORIZON_SHORT = 1,
  INVESTMENT_HORIZON_MID = 2,
  INVESTMENT_HORIZON_LONG = 3,
  UNRECOGNIZED = -1,
}

export function investmentHorizonFromJSON(object: any): InvestmentHorizon {
  switch (object) {
    case 0:
    case "INVESTMENT_HORIZON_UNSPECIFIED":
      return InvestmentHorizon.INVESTMENT_HORIZON_UNSPECIFIED;
    case 1:
    case "INVESTMENT_HORIZON_SHORT":
      return InvestmentHorizon.INVESTMENT_HORIZON_SHORT;
    case 2:
    case "INVESTMENT_HORIZON_MID":
      return InvestmentHorizon.INVESTMENT_HORIZON_MID;
    case 3:
    case "INVESTMENT_HORIZON_LONG":
      return InvestmentHorizon.INVESTMENT_HORIZON_LONG;
    case -1:
    case "UNRECOGNIZED":
    default:
      return InvestmentHorizon.UNRECOGNIZED;
  }
}

export function investmentHorizonToJSON(object: InvestmentHorizon): string {
  switch (object) {
    case InvestmentHorizon.INVESTMENT_HORIZON_UNSPECIFIED:
      return "INVESTMENT_HORIZON_UNSPECIFIED";
    case InvestmentHorizon.INVESTMENT_HORIZON_SHORT:
      return "INVESTMENT_HORIZON_SHORT";
    case InvestmentHorizon.INVESTMENT_HORIZON_MID:
      return "INVESTMENT_HORIZON_MID";
    case InvestmentHorizon.INVESTMENT_HORIZON_LONG:
      return "INVESTMENT_HORIZON_LONG";
    case InvestmentHorizon.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ValidationStatus {
  VALIDATION_STATUS_UNSPECIFIED = 0,
  VALIDATION_STATUS_PASSED = 1,
  VALIDATION_STATUS_RETRIED = 2,
  VALIDATION_STATUS_FAILED = 3,
  UNRECOGNIZED = -1,
}

export function validationStatusFromJSON(object: any): ValidationStatus {
  switch (object) {
    case 0:
    case "VALIDATION_STATUS_UNSPECIFIED":
      return ValidationStatus.VALIDATION_STATUS_UNSPECIFIED;
    case 1:
    case "VALIDATION_STATUS_PASSED":
      return ValidationStatus.VALIDATION_STATUS_PASSED;
    case 2:
    case "VALIDATION_STATUS_RETRIED":
      return ValidationStatus.VALIDATION_STATUS_RETRIED;
    case 3:
    case "VALIDATION_STATUS_FAILED":
      return ValidationStatus.VALIDATION_STATUS_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ValidationStatus.UNRECOGNIZED;
  }
}

export function validationStatusToJSON(object: ValidationStatus): string {
  switch (object) {
    case ValidationStatus.VALIDATION_STATUS_UNSPECIFIED:
      return "VALIDATION_STATUS_UNSPECIFIED";
    case ValidationStatus.VALIDATION_STATUS_PASSED:
      return "VALIDATION_STATUS_PASSED";
    case ValidationStatus.VALIDATION_STATUS_RETRIED:
      return "VALIDATION_STATUS_RETRIED";
    case ValidationStatus.VALIDATION_STATUS_FAILED:
      return "VALIDATION_STATUS_FAILED";
    case ValidationStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface GetRecommendationsRequest {
  riskTolerance: RiskTolerance;
  investmentHorizon: InvestmentHorizon;
  preferredSectors: string[];
  /** 비어 있으면 risk_tolerance, investment_horizon, preferred_sectors로 검색어를 만든다. */
  freeTextQuery: string;
}

export interface GetRecommendationsResponse {
  recommendations: Recommendation[];
  validationStatus: ValidationStatus;
  validationErrors: string[];
  retryCount: number;
}

export interface Recommendation {
  stockCode: string;
  nameKr: string;
  /** 0.0 ~ 1.0 */
  fitScore: number;
  narrative: string;
  improvementTags: string[];
  /** 추천 시점 가격. 가격이 없으면 proto3 기본값 0을 반환한다. */
  priceSnapshot: string;
}

function createBaseGetRecommendationsRequest(): GetRecommendationsRequest {
  return { riskTolerance: 0, investmentHorizon: 0, preferredSectors: [], freeTextQuery: "" };
}

export const GetRecommendationsRequest: MessageFns<GetRecommendationsRequest> = {
  encode(message: GetRecommendationsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.riskTolerance !== 0) {
      writer.uint32(8).int32(message.riskTolerance);
    }
    if (message.investmentHorizon !== 0) {
      writer.uint32(16).int32(message.investmentHorizon);
    }
    for (const v of message.preferredSectors) {
      writer.uint32(26).string(v!);
    }
    if (message.freeTextQuery !== "") {
      writer.uint32(34).string(message.freeTextQuery);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRecommendationsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRecommendationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.riskTolerance = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.investmentHorizon = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.preferredSectors.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.freeTextQuery = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRecommendationsRequest {
    return {
      riskTolerance: isSet(object.riskTolerance)
        ? riskToleranceFromJSON(object.riskTolerance)
        : isSet(object.risk_tolerance)
        ? riskToleranceFromJSON(object.risk_tolerance)
        : 0,
      investmentHorizon: isSet(object.investmentHorizon)
        ? investmentHorizonFromJSON(object.investmentHorizon)
        : isSet(object.investment_horizon)
        ? investmentHorizonFromJSON(object.investment_horizon)
        : 0,
      preferredSectors: globalThis.Array.isArray(object?.preferredSectors)
        ? object.preferredSectors.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.preferred_sectors)
        ? object.preferred_sectors.map((e: any) => globalThis.String(e))
        : [],
      freeTextQuery: isSet(object.freeTextQuery)
        ? globalThis.String(object.freeTextQuery)
        : isSet(object.free_text_query)
        ? globalThis.String(object.free_text_query)
        : "",
    };
  },

  toJSON(message: GetRecommendationsRequest): unknown {
    const obj: any = {};
    if (message.riskTolerance !== 0) {
      obj.riskTolerance = riskToleranceToJSON(message.riskTolerance);
    }
    if (message.investmentHorizon !== 0) {
      obj.investmentHorizon = investmentHorizonToJSON(message.investmentHorizon);
    }
    if (message.preferredSectors?.length) {
      obj.preferredSectors = message.preferredSectors;
    }
    if (message.freeTextQuery !== "") {
      obj.freeTextQuery = message.freeTextQuery;
    }
    return obj;
  },

  create(base?: DeepPartial<GetRecommendationsRequest>): GetRecommendationsRequest {
    return GetRecommendationsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetRecommendationsRequest>): GetRecommendationsRequest {
    const message = createBaseGetRecommendationsRequest();
    message.riskTolerance = object.riskTolerance ?? 0;
    message.investmentHorizon = object.investmentHorizon ?? 0;
    message.preferredSectors = object.preferredSectors?.map((e) => e) || [];
    message.freeTextQuery = object.freeTextQuery ?? "";
    return message;
  },
};

function createBaseGetRecommendationsResponse(): GetRecommendationsResponse {
  return { recommendations: [], validationStatus: 0, validationErrors: [], retryCount: 0 };
}

export const GetRecommendationsResponse: MessageFns<GetRecommendationsResponse> = {
  encode(message: GetRecommendationsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.recommendations) {
      Recommendation.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.validationStatus !== 0) {
      writer.uint32(16).int32(message.validationStatus);
    }
    for (const v of message.validationErrors) {
      writer.uint32(26).string(v!);
    }
    if (message.retryCount !== 0) {
      writer.uint32(32).int32(message.retryCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRecommendationsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRecommendationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.recommendations.push(Recommendation.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.validationStatus = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.validationErrors.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.retryCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRecommendationsResponse {
    return {
      recommendations: globalThis.Array.isArray(object?.recommendations)
        ? object.recommendations.map((e: any) => Recommendation.fromJSON(e))
        : [],
      validationStatus: isSet(object.validationStatus)
        ? validationStatusFromJSON(object.validationStatus)
        : isSet(object.validation_status)
        ? validationStatusFromJSON(object.validation_status)
        : 0,
      validationErrors: globalThis.Array.isArray(object?.validationErrors)
        ? object.validationErrors.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.validation_errors)
        ? object.validation_errors.map((e: any) => globalThis.String(e))
        : [],
      retryCount: isSet(object.retryCount)
        ? globalThis.Number(object.retryCount)
        : isSet(object.retry_count)
        ? globalThis.Number(object.retry_count)
        : 0,
    };
  },

  toJSON(message: GetRecommendationsResponse): unknown {
    const obj: any = {};
    if (message.recommendations?.length) {
      obj.recommendations = message.recommendations.map((e) => Recommendation.toJSON(e));
    }
    if (message.validationStatus !== 0) {
      obj.validationStatus = validationStatusToJSON(message.validationStatus);
    }
    if (message.validationErrors?.length) {
      obj.validationErrors = message.validationErrors;
    }
    if (message.retryCount !== 0) {
      obj.retryCount = Math.round(message.retryCount);
    }
    return obj;
  },

  create(base?: DeepPartial<GetRecommendationsResponse>): GetRecommendationsResponse {
    return GetRecommendationsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetRecommendationsResponse>): GetRecommendationsResponse {
    const message = createBaseGetRecommendationsResponse();
    message.recommendations = object.recommendations?.map((e) => Recommendation.fromPartial(e)) || [];
    message.validationStatus = object.validationStatus ?? 0;
    message.validationErrors = object.validationErrors?.map((e) => e) || [];
    message.retryCount = object.retryCount ?? 0;
    return message;
  },
};

function createBaseRecommendation(): Recommendation {
  return { stockCode: "", nameKr: "", fitScore: 0, narrative: "", improvementTags: [], priceSnapshot: "0" };
}

export const Recommendation: MessageFns<Recommendation> = {
  encode(message: Recommendation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stockCode !== "") {
      writer.uint32(10).string(message.stockCode);
    }
    if (message.nameKr !== "") {
      writer.uint32(18).string(message.nameKr);
    }
    if (message.fitScore !== 0) {
      writer.uint32(25).double(message.fitScore);
    }
    if (message.narrative !== "") {
      writer.uint32(34).string(message.narrative);
    }
    for (const v of message.improvementTags) {
      writer.uint32(42).string(v!);
    }
    if (message.priceSnapshot !== "0") {
      writer.uint32(48).int64(message.priceSnapshot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Recommendation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecommendation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stockCode = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nameKr = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.fitScore = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.narrative = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.improvementTags.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.priceSnapshot = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Recommendation {
    return {
      stockCode: isSet(object.stockCode)
        ? globalThis.String(object.stockCode)
        : isSet(object.stock_code)
        ? globalThis.String(object.stock_code)
        : "",
      nameKr: isSet(object.nameKr)
        ? globalThis.String(object.nameKr)
        : isSet(object.name_kr)
        ? globalThis.String(object.name_kr)
        : "",
      fitScore: isSet(object.fitScore)
        ? globalThis.Number(object.fitScore)
        : isSet(object.fit_score)
        ? globalThis.Number(object.fit_score)
        : 0,
      narrative: isSet(object.narrative) ? globalThis.String(object.narrative) : "",
      improvementTags: globalThis.Array.isArray(object?.improvementTags)
        ? object.improvementTags.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.improvement_tags)
        ? object.improvement_tags.map((e: any) => globalThis.String(e))
        : [],
      priceSnapshot: isSet(object.priceSnapshot)
        ? globalThis.String(object.priceSnapshot)
        : isSet(object.price_snapshot)
        ? globalThis.String(object.price_snapshot)
        : "0",
    };
  },

  toJSON(message: Recommendation): unknown {
    const obj: any = {};
    if (message.stockCode !== "") {
      obj.stockCode = message.stockCode;
    }
    if (message.nameKr !== "") {
      obj.nameKr = message.nameKr;
    }
    if (message.fitScore !== 0) {
      obj.fitScore = message.fitScore;
    }
    if (message.narrative !== "") {
      obj.narrative = message.narrative;
    }
    if (message.improvementTags?.length) {
      obj.improvementTags = message.improvementTags;
    }
    if (message.priceSnapshot !== "0") {
      obj.priceSnapshot = message.priceSnapshot;
    }
    return obj;
  },

  create(base?: DeepPartial<Recommendation>): Recommendation {
    return Recommendation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Recommendation>): Recommendation {
    const message = createBaseRecommendation();
    message.stockCode = object.stockCode ?? "";
    message.nameKr = object.nameKr ?? "";
    message.fitScore = object.fitScore ?? 0;
    message.narrative = object.narrative ?? "";
    message.improvementTags = object.improvementTags?.map((e) => e) || [];
    message.priceSnapshot = object.priceSnapshot ?? "0";
    return message;
  },
};

export type AdvisoryServiceDefinition = typeof AdvisoryServiceDefinition;
export const AdvisoryServiceDefinition = {
  name: "AdvisoryService",
  fullName: "candle.advisory.v1.AdvisoryService",
  methods: {
    /** 사용자 투자성향과 자연어 질의를 기반으로 최대 5개 종목을 추천한다. */
    getRecommendations: {
      name: "GetRecommendations",
      requestType: GetRecommendationsRequest as typeof GetRecommendationsRequest,
      requestStream: false,
      responseType: GetRecommendationsResponse as typeof GetRecommendationsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface AdvisoryServiceImplementation<CallContextExt = {}> {
  /** 사용자 투자성향과 자연어 질의를 기반으로 최대 5개 종목을 추천한다. */
  getRecommendations(
    request: GetRecommendationsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetRecommendationsResponse>>;
}

export interface AdvisoryServiceClient<CallOptionsExt = {}> {
  /** 사용자 투자성향과 자연어 질의를 기반으로 최대 5개 종목을 추천한다. */
  getRecommendations(
    request: DeepPartial<GetRecommendationsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetRecommendationsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
