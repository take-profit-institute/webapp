// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: candle/trading/v1/trading.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { CommandMetadata, PageRequest, PageResponse } from "../../common/v1/common";

export const protobufPackage = "candle.trading.v1";

export enum OrderSide {
  ORDER_SIDE_UNSPECIFIED = 0,
  ORDER_SIDE_BUY = 1,
  ORDER_SIDE_SELL = 2,
  UNRECOGNIZED = -1,
}

export function orderSideFromJSON(object: any): OrderSide {
  switch (object) {
    case 0:
    case "ORDER_SIDE_UNSPECIFIED":
      return OrderSide.ORDER_SIDE_UNSPECIFIED;
    case 1:
    case "ORDER_SIDE_BUY":
      return OrderSide.ORDER_SIDE_BUY;
    case 2:
    case "ORDER_SIDE_SELL":
      return OrderSide.ORDER_SIDE_SELL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return OrderSide.UNRECOGNIZED;
  }
}

export function orderSideToJSON(object: OrderSide): string {
  switch (object) {
    case OrderSide.ORDER_SIDE_UNSPECIFIED:
      return "ORDER_SIDE_UNSPECIFIED";
    case OrderSide.ORDER_SIDE_BUY:
      return "ORDER_SIDE_BUY";
    case OrderSide.ORDER_SIDE_SELL:
      return "ORDER_SIDE_SELL";
    case OrderSide.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum OrderKind {
  ORDER_KIND_UNSPECIFIED = 0,
  ORDER_KIND_MARKET = 1,
  ORDER_KIND_LIMIT = 2,
  ORDER_KIND_AFTER_HOURS_CLOSE = 3,
  UNRECOGNIZED = -1,
}

export function orderKindFromJSON(object: any): OrderKind {
  switch (object) {
    case 0:
    case "ORDER_KIND_UNSPECIFIED":
      return OrderKind.ORDER_KIND_UNSPECIFIED;
    case 1:
    case "ORDER_KIND_MARKET":
      return OrderKind.ORDER_KIND_MARKET;
    case 2:
    case "ORDER_KIND_LIMIT":
      return OrderKind.ORDER_KIND_LIMIT;
    case 3:
    case "ORDER_KIND_AFTER_HOURS_CLOSE":
      return OrderKind.ORDER_KIND_AFTER_HOURS_CLOSE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return OrderKind.UNRECOGNIZED;
  }
}

export function orderKindToJSON(object: OrderKind): string {
  switch (object) {
    case OrderKind.ORDER_KIND_UNSPECIFIED:
      return "ORDER_KIND_UNSPECIFIED";
    case OrderKind.ORDER_KIND_MARKET:
      return "ORDER_KIND_MARKET";
    case OrderKind.ORDER_KIND_LIMIT:
      return "ORDER_KIND_LIMIT";
    case OrderKind.ORDER_KIND_AFTER_HOURS_CLOSE:
      return "ORDER_KIND_AFTER_HOURS_CLOSE";
    case OrderKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum OrderStatus {
  ORDER_STATUS_UNSPECIFIED = 0,
  ORDER_STATUS_PENDING = 1,
  ORDER_STATUS_FILLED = 2,
  ORDER_STATUS_CANCELLED = 3,
  ORDER_STATUS_REJECTED = 4,
  UNRECOGNIZED = -1,
}

export function orderStatusFromJSON(object: any): OrderStatus {
  switch (object) {
    case 0:
    case "ORDER_STATUS_UNSPECIFIED":
      return OrderStatus.ORDER_STATUS_UNSPECIFIED;
    case 1:
    case "ORDER_STATUS_PENDING":
      return OrderStatus.ORDER_STATUS_PENDING;
    case 2:
    case "ORDER_STATUS_FILLED":
      return OrderStatus.ORDER_STATUS_FILLED;
    case 3:
    case "ORDER_STATUS_CANCELLED":
      return OrderStatus.ORDER_STATUS_CANCELLED;
    case 4:
    case "ORDER_STATUS_REJECTED":
      return OrderStatus.ORDER_STATUS_REJECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return OrderStatus.UNRECOGNIZED;
  }
}

export function orderStatusToJSON(object: OrderStatus): string {
  switch (object) {
    case OrderStatus.ORDER_STATUS_UNSPECIFIED:
      return "ORDER_STATUS_UNSPECIFIED";
    case OrderStatus.ORDER_STATUS_PENDING:
      return "ORDER_STATUS_PENDING";
    case OrderStatus.ORDER_STATUS_FILLED:
      return "ORDER_STATUS_FILLED";
    case OrderStatus.ORDER_STATUS_CANCELLED:
      return "ORDER_STATUS_CANCELLED";
    case OrderStatus.ORDER_STATUS_REJECTED:
      return "ORDER_STATUS_REJECTED";
    case OrderStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ReservationTiming {
  RESERVATION_TIMING_UNSPECIFIED = 0,
  RESERVATION_TIMING_OPEN = 1,
  RESERVATION_TIMING_PREV_CLOSE = 2,
  RESERVATION_TIMING_TODAY_CLOSE = 3,
  UNRECOGNIZED = -1,
}

export function reservationTimingFromJSON(object: any): ReservationTiming {
  switch (object) {
    case 0:
    case "RESERVATION_TIMING_UNSPECIFIED":
      return ReservationTiming.RESERVATION_TIMING_UNSPECIFIED;
    case 1:
    case "RESERVATION_TIMING_OPEN":
      return ReservationTiming.RESERVATION_TIMING_OPEN;
    case 2:
    case "RESERVATION_TIMING_PREV_CLOSE":
      return ReservationTiming.RESERVATION_TIMING_PREV_CLOSE;
    case 3:
    case "RESERVATION_TIMING_TODAY_CLOSE":
      return ReservationTiming.RESERVATION_TIMING_TODAY_CLOSE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ReservationTiming.UNRECOGNIZED;
  }
}

export function reservationTimingToJSON(object: ReservationTiming): string {
  switch (object) {
    case ReservationTiming.RESERVATION_TIMING_UNSPECIFIED:
      return "RESERVATION_TIMING_UNSPECIFIED";
    case ReservationTiming.RESERVATION_TIMING_OPEN:
      return "RESERVATION_TIMING_OPEN";
    case ReservationTiming.RESERVATION_TIMING_PREV_CLOSE:
      return "RESERVATION_TIMING_PREV_CLOSE";
    case ReservationTiming.RESERVATION_TIMING_TODAY_CLOSE:
      return "RESERVATION_TIMING_TODAY_CLOSE";
    case ReservationTiming.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ReservationStatus {
  RESERVATION_STATUS_UNSPECIFIED = 0,
  RESERVATION_STATUS_RESERVED = 1,
  RESERVATION_STATUS_EXECUTED = 2,
  RESERVATION_STATUS_CANCELLED = 3,
  UNRECOGNIZED = -1,
}

export function reservationStatusFromJSON(object: any): ReservationStatus {
  switch (object) {
    case 0:
    case "RESERVATION_STATUS_UNSPECIFIED":
      return ReservationStatus.RESERVATION_STATUS_UNSPECIFIED;
    case 1:
    case "RESERVATION_STATUS_RESERVED":
      return ReservationStatus.RESERVATION_STATUS_RESERVED;
    case 2:
    case "RESERVATION_STATUS_EXECUTED":
      return ReservationStatus.RESERVATION_STATUS_EXECUTED;
    case 3:
    case "RESERVATION_STATUS_CANCELLED":
      return ReservationStatus.RESERVATION_STATUS_CANCELLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ReservationStatus.UNRECOGNIZED;
  }
}

export function reservationStatusToJSON(object: ReservationStatus): string {
  switch (object) {
    case ReservationStatus.RESERVATION_STATUS_UNSPECIFIED:
      return "RESERVATION_STATUS_UNSPECIFIED";
    case ReservationStatus.RESERVATION_STATUS_RESERVED:
      return "RESERVATION_STATUS_RESERVED";
    case ReservationStatus.RESERVATION_STATUS_EXECUTED:
      return "RESERVATION_STATUS_EXECUTED";
    case ReservationStatus.RESERVATION_STATUS_CANCELLED:
      return "RESERVATION_STATUS_CANCELLED";
    case ReservationStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface AccountBalance {
  userId: string;
  cash: string;
  reservedBalance: string;
  availableCash: string;
  totalAsset: string;
}

export interface Holding {
  symbol: string;
  quantity: string;
  averagePrice: string;
  realizedProfit: string;
  active: boolean;
}

export interface Order {
  id: string;
  userId: string;
  symbol: string;
  side: OrderSide;
  kind: OrderKind;
  quantity: string;
  price: string;
  status: OrderStatus;
  parentOrderId: string;
  reservedAmount: string;
  createdAt?: Date | undefined;
}

export interface Reservation {
  id: string;
  userId: string;
  symbol: string;
  side: OrderSide;
  timing: ReservationTiming;
  kind: OrderKind;
  quantity: string;
  price: string;
  scheduledDate: string;
  reservedAmount: string;
  status: ReservationStatus;
  parentOrderId: string;
  createdAt?: Date | undefined;
}

export interface GetBalanceRequest {
  userId: string;
}

export interface GetBalanceResponse {
  balance?: AccountBalance | undefined;
}

export interface ListHoldingsRequest {
  userId: string;
  includeInactive: boolean;
  page?: PageRequest | undefined;
}

export interface ListHoldingsResponse {
  holdings: Holding[];
  page?: PageResponse | undefined;
}

export interface ListOrdersRequest {
  userId: string;
  status: OrderStatus;
  page?: PageRequest | undefined;
}

export interface ListOrdersResponse {
  orders: Order[];
  page?: PageResponse | undefined;
}

export interface PlaceOrderRequest {
  userId: string;
  symbol: string;
  side: OrderSide;
  kind: OrderKind;
  quantity: string;
  price: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface PlaceOrderResponse {
  order?: Order | undefined;
}

export interface CancelOrderRequest {
  userId: string;
  orderId: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface CancelOrderResponse {
  order?: Order | undefined;
  releasedAmount: string;
}

/** 정정 = 원주문 취소 후 parent_order_id를 가진 새 주문 생성(CAN-005/007/008). */
export interface AmendOrderRequest {
  userId: string;
  orderId: string;
  quantity: string;
  price: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface AmendOrderResponse {
  order?: Order | undefined;
}

export interface ListReservationsRequest {
  userId: string;
  status: ReservationStatus;
  page?: PageRequest | undefined;
}

export interface ListReservationsResponse {
  reservations: Reservation[];
  page?: PageResponse | undefined;
}

export interface PlaceReservationRequest {
  userId: string;
  symbol: string;
  side: OrderSide;
  timing: ReservationTiming;
  kind: OrderKind;
  quantity: string;
  price: string;
  scheduledDate: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface PlaceReservationResponse {
  reservation?: Reservation | undefined;
}

export interface CancelReservationRequest {
  userId: string;
  reservationId: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface CancelReservationResponse {
  reservation?: Reservation | undefined;
  releasedAmount: string;
}

/** 정정 = 원예약 취소 후 parent_order_id를 가진 새 예약 생성(CAN-006~008). */
export interface AmendReservationRequest {
  userId: string;
  reservationId: string;
  timing: ReservationTiming;
  kind: OrderKind;
  quantity: string;
  price: string;
  scheduledDate: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface AmendReservationResponse {
  reservation?: Reservation | undefined;
}

function createBaseAccountBalance(): AccountBalance {
  return { userId: "", cash: "0", reservedBalance: "0", availableCash: "0", totalAsset: "0" };
}

export const AccountBalance: MessageFns<AccountBalance> = {
  encode(message: AccountBalance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.cash !== "0") {
      writer.uint32(16).int64(message.cash);
    }
    if (message.reservedBalance !== "0") {
      writer.uint32(24).int64(message.reservedBalance);
    }
    if (message.availableCash !== "0") {
      writer.uint32(32).int64(message.availableCash);
    }
    if (message.totalAsset !== "0") {
      writer.uint32(40).int64(message.totalAsset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AccountBalance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountBalance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.cash = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.reservedBalance = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.availableCash = reader.int64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.totalAsset = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountBalance {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      cash: isSet(object.cash) ? globalThis.String(object.cash) : "0",
      reservedBalance: isSet(object.reservedBalance)
        ? globalThis.String(object.reservedBalance)
        : isSet(object.reserved_balance)
        ? globalThis.String(object.reserved_balance)
        : "0",
      availableCash: isSet(object.availableCash)
        ? globalThis.String(object.availableCash)
        : isSet(object.available_cash)
        ? globalThis.String(object.available_cash)
        : "0",
      totalAsset: isSet(object.totalAsset)
        ? globalThis.String(object.totalAsset)
        : isSet(object.total_asset)
        ? globalThis.String(object.total_asset)
        : "0",
    };
  },

  toJSON(message: AccountBalance): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.cash !== "0") {
      obj.cash = message.cash;
    }
    if (message.reservedBalance !== "0") {
      obj.reservedBalance = message.reservedBalance;
    }
    if (message.availableCash !== "0") {
      obj.availableCash = message.availableCash;
    }
    if (message.totalAsset !== "0") {
      obj.totalAsset = message.totalAsset;
    }
    return obj;
  },

  create(base?: DeepPartial<AccountBalance>): AccountBalance {
    return AccountBalance.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AccountBalance>): AccountBalance {
    const message = createBaseAccountBalance();
    message.userId = object.userId ?? "";
    message.cash = object.cash ?? "0";
    message.reservedBalance = object.reservedBalance ?? "0";
    message.availableCash = object.availableCash ?? "0";
    message.totalAsset = object.totalAsset ?? "0";
    return message;
  },
};

function createBaseHolding(): Holding {
  return { symbol: "", quantity: "0", averagePrice: "0", realizedProfit: "0", active: false };
}

export const Holding: MessageFns<Holding> = {
  encode(message: Holding, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.symbol !== "") {
      writer.uint32(10).string(message.symbol);
    }
    if (message.quantity !== "0") {
      writer.uint32(16).int64(message.quantity);
    }
    if (message.averagePrice !== "0") {
      writer.uint32(24).int64(message.averagePrice);
    }
    if (message.realizedProfit !== "0") {
      writer.uint32(32).int64(message.realizedProfit);
    }
    if (message.active !== false) {
      writer.uint32(40).bool(message.active);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Holding {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHolding();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.quantity = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.averagePrice = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.realizedProfit = reader.int64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.active = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Holding {
    return {
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "0",
      averagePrice: isSet(object.averagePrice)
        ? globalThis.String(object.averagePrice)
        : isSet(object.average_price)
        ? globalThis.String(object.average_price)
        : "0",
      realizedProfit: isSet(object.realizedProfit)
        ? globalThis.String(object.realizedProfit)
        : isSet(object.realized_profit)
        ? globalThis.String(object.realized_profit)
        : "0",
      active: isSet(object.active) ? globalThis.Boolean(object.active) : false,
    };
  },

  toJSON(message: Holding): unknown {
    const obj: any = {};
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.quantity !== "0") {
      obj.quantity = message.quantity;
    }
    if (message.averagePrice !== "0") {
      obj.averagePrice = message.averagePrice;
    }
    if (message.realizedProfit !== "0") {
      obj.realizedProfit = message.realizedProfit;
    }
    if (message.active !== false) {
      obj.active = message.active;
    }
    return obj;
  },

  create(base?: DeepPartial<Holding>): Holding {
    return Holding.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Holding>): Holding {
    const message = createBaseHolding();
    message.symbol = object.symbol ?? "";
    message.quantity = object.quantity ?? "0";
    message.averagePrice = object.averagePrice ?? "0";
    message.realizedProfit = object.realizedProfit ?? "0";
    message.active = object.active ?? false;
    return message;
  },
};

function createBaseOrder(): Order {
  return {
    id: "",
    userId: "",
    symbol: "",
    side: 0,
    kind: 0,
    quantity: "0",
    price: "0",
    status: 0,
    parentOrderId: "",
    reservedAmount: "0",
    createdAt: undefined,
  };
}

export const Order: MessageFns<Order> = {
  encode(message: Order, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.symbol !== "") {
      writer.uint32(26).string(message.symbol);
    }
    if (message.side !== 0) {
      writer.uint32(32).int32(message.side);
    }
    if (message.kind !== 0) {
      writer.uint32(40).int32(message.kind);
    }
    if (message.quantity !== "0") {
      writer.uint32(48).int64(message.quantity);
    }
    if (message.price !== "0") {
      writer.uint32(56).int64(message.price);
    }
    if (message.status !== 0) {
      writer.uint32(64).int32(message.status);
    }
    if (message.parentOrderId !== "") {
      writer.uint32(74).string(message.parentOrderId);
    }
    if (message.reservedAmount !== "0") {
      writer.uint32(80).int64(message.reservedAmount);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Order {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOrder();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.side = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.quantity = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.price = reader.int64().toString();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.parentOrderId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.reservedAmount = reader.int64().toString();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Order {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      side: isSet(object.side) ? orderSideFromJSON(object.side) : 0,
      kind: isSet(object.kind) ? orderKindFromJSON(object.kind) : 0,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "0",
      price: isSet(object.price) ? globalThis.String(object.price) : "0",
      status: isSet(object.status) ? orderStatusFromJSON(object.status) : 0,
      parentOrderId: isSet(object.parentOrderId)
        ? globalThis.String(object.parentOrderId)
        : isSet(object.parent_order_id)
        ? globalThis.String(object.parent_order_id)
        : "",
      reservedAmount: isSet(object.reservedAmount)
        ? globalThis.String(object.reservedAmount)
        : isSet(object.reserved_amount)
        ? globalThis.String(object.reserved_amount)
        : "0",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: Order): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.side !== 0) {
      obj.side = orderSideToJSON(message.side);
    }
    if (message.kind !== 0) {
      obj.kind = orderKindToJSON(message.kind);
    }
    if (message.quantity !== "0") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "0") {
      obj.price = message.price;
    }
    if (message.status !== 0) {
      obj.status = orderStatusToJSON(message.status);
    }
    if (message.parentOrderId !== "") {
      obj.parentOrderId = message.parentOrderId;
    }
    if (message.reservedAmount !== "0") {
      obj.reservedAmount = message.reservedAmount;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Order>): Order {
    return Order.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Order>): Order {
    const message = createBaseOrder();
    message.id = object.id ?? "";
    message.userId = object.userId ?? "";
    message.symbol = object.symbol ?? "";
    message.side = object.side ?? 0;
    message.kind = object.kind ?? 0;
    message.quantity = object.quantity ?? "0";
    message.price = object.price ?? "0";
    message.status = object.status ?? 0;
    message.parentOrderId = object.parentOrderId ?? "";
    message.reservedAmount = object.reservedAmount ?? "0";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseReservation(): Reservation {
  return {
    id: "",
    userId: "",
    symbol: "",
    side: 0,
    timing: 0,
    kind: 0,
    quantity: "0",
    price: "0",
    scheduledDate: "",
    reservedAmount: "0",
    status: 0,
    parentOrderId: "",
    createdAt: undefined,
  };
}

export const Reservation: MessageFns<Reservation> = {
  encode(message: Reservation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.symbol !== "") {
      writer.uint32(26).string(message.symbol);
    }
    if (message.side !== 0) {
      writer.uint32(32).int32(message.side);
    }
    if (message.timing !== 0) {
      writer.uint32(40).int32(message.timing);
    }
    if (message.kind !== 0) {
      writer.uint32(48).int32(message.kind);
    }
    if (message.quantity !== "0") {
      writer.uint32(56).int64(message.quantity);
    }
    if (message.price !== "0") {
      writer.uint32(64).int64(message.price);
    }
    if (message.scheduledDate !== "") {
      writer.uint32(74).string(message.scheduledDate);
    }
    if (message.reservedAmount !== "0") {
      writer.uint32(80).int64(message.reservedAmount);
    }
    if (message.status !== 0) {
      writer.uint32(88).int32(message.status);
    }
    if (message.parentOrderId !== "") {
      writer.uint32(98).string(message.parentOrderId);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Reservation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReservation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.side = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.timing = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.quantity = reader.int64().toString();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.price = reader.int64().toString();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.scheduledDate = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.reservedAmount = reader.int64().toString();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.parentOrderId = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Reservation {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      side: isSet(object.side) ? orderSideFromJSON(object.side) : 0,
      timing: isSet(object.timing) ? reservationTimingFromJSON(object.timing) : 0,
      kind: isSet(object.kind) ? orderKindFromJSON(object.kind) : 0,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "0",
      price: isSet(object.price) ? globalThis.String(object.price) : "0",
      scheduledDate: isSet(object.scheduledDate)
        ? globalThis.String(object.scheduledDate)
        : isSet(object.scheduled_date)
        ? globalThis.String(object.scheduled_date)
        : "",
      reservedAmount: isSet(object.reservedAmount)
        ? globalThis.String(object.reservedAmount)
        : isSet(object.reserved_amount)
        ? globalThis.String(object.reserved_amount)
        : "0",
      status: isSet(object.status) ? reservationStatusFromJSON(object.status) : 0,
      parentOrderId: isSet(object.parentOrderId)
        ? globalThis.String(object.parentOrderId)
        : isSet(object.parent_order_id)
        ? globalThis.String(object.parent_order_id)
        : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: Reservation): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.side !== 0) {
      obj.side = orderSideToJSON(message.side);
    }
    if (message.timing !== 0) {
      obj.timing = reservationTimingToJSON(message.timing);
    }
    if (message.kind !== 0) {
      obj.kind = orderKindToJSON(message.kind);
    }
    if (message.quantity !== "0") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "0") {
      obj.price = message.price;
    }
    if (message.scheduledDate !== "") {
      obj.scheduledDate = message.scheduledDate;
    }
    if (message.reservedAmount !== "0") {
      obj.reservedAmount = message.reservedAmount;
    }
    if (message.status !== 0) {
      obj.status = reservationStatusToJSON(message.status);
    }
    if (message.parentOrderId !== "") {
      obj.parentOrderId = message.parentOrderId;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Reservation>): Reservation {
    return Reservation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Reservation>): Reservation {
    const message = createBaseReservation();
    message.id = object.id ?? "";
    message.userId = object.userId ?? "";
    message.symbol = object.symbol ?? "";
    message.side = object.side ?? 0;
    message.timing = object.timing ?? 0;
    message.kind = object.kind ?? 0;
    message.quantity = object.quantity ?? "0";
    message.price = object.price ?? "0";
    message.scheduledDate = object.scheduledDate ?? "";
    message.reservedAmount = object.reservedAmount ?? "0";
    message.status = object.status ?? 0;
    message.parentOrderId = object.parentOrderId ?? "";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseGetBalanceRequest(): GetBalanceRequest {
  return { userId: "" };
}

export const GetBalanceRequest: MessageFns<GetBalanceRequest> = {
  encode(message: GetBalanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBalanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBalanceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBalanceRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
    };
  },

  toJSON(message: GetBalanceRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetBalanceRequest>): GetBalanceRequest {
    return GetBalanceRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBalanceRequest>): GetBalanceRequest {
    const message = createBaseGetBalanceRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetBalanceResponse(): GetBalanceResponse {
  return { balance: undefined };
}

export const GetBalanceResponse: MessageFns<GetBalanceResponse> = {
  encode(message: GetBalanceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.balance !== undefined) {
      AccountBalance.encode(message.balance, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBalanceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBalanceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.balance = AccountBalance.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBalanceResponse {
    return { balance: isSet(object.balance) ? AccountBalance.fromJSON(object.balance) : undefined };
  },

  toJSON(message: GetBalanceResponse): unknown {
    const obj: any = {};
    if (message.balance !== undefined) {
      obj.balance = AccountBalance.toJSON(message.balance);
    }
    return obj;
  },

  create(base?: DeepPartial<GetBalanceResponse>): GetBalanceResponse {
    return GetBalanceResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBalanceResponse>): GetBalanceResponse {
    const message = createBaseGetBalanceResponse();
    message.balance = (object.balance !== undefined && object.balance !== null)
      ? AccountBalance.fromPartial(object.balance)
      : undefined;
    return message;
  },
};

function createBaseListHoldingsRequest(): ListHoldingsRequest {
  return { userId: "", includeInactive: false, page: undefined };
}

export const ListHoldingsRequest: MessageFns<ListHoldingsRequest> = {
  encode(message: ListHoldingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.includeInactive !== false) {
      writer.uint32(16).bool(message.includeInactive);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListHoldingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListHoldingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.includeInactive = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListHoldingsRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      includeInactive: isSet(object.includeInactive)
        ? globalThis.Boolean(object.includeInactive)
        : isSet(object.include_inactive)
        ? globalThis.Boolean(object.include_inactive)
        : false,
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListHoldingsRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.includeInactive !== false) {
      obj.includeInactive = message.includeInactive;
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListHoldingsRequest>): ListHoldingsRequest {
    return ListHoldingsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListHoldingsRequest>): ListHoldingsRequest {
    const message = createBaseListHoldingsRequest();
    message.userId = object.userId ?? "";
    message.includeInactive = object.includeInactive ?? false;
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListHoldingsResponse(): ListHoldingsResponse {
  return { holdings: [], page: undefined };
}

export const ListHoldingsResponse: MessageFns<ListHoldingsResponse> = {
  encode(message: ListHoldingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.holdings) {
      Holding.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListHoldingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListHoldingsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.holdings.push(Holding.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListHoldingsResponse {
    return {
      holdings: globalThis.Array.isArray(object?.holdings) ? object.holdings.map((e: any) => Holding.fromJSON(e)) : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListHoldingsResponse): unknown {
    const obj: any = {};
    if (message.holdings?.length) {
      obj.holdings = message.holdings.map((e) => Holding.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListHoldingsResponse>): ListHoldingsResponse {
    return ListHoldingsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListHoldingsResponse>): ListHoldingsResponse {
    const message = createBaseListHoldingsResponse();
    message.holdings = object.holdings?.map((e) => Holding.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListOrdersRequest(): ListOrdersRequest {
  return { userId: "", status: 0, page: undefined };
}

export const ListOrdersRequest: MessageFns<ListOrdersRequest> = {
  encode(message: ListOrdersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListOrdersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListOrdersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListOrdersRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      status: isSet(object.status) ? orderStatusFromJSON(object.status) : 0,
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListOrdersRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.status !== 0) {
      obj.status = orderStatusToJSON(message.status);
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListOrdersRequest>): ListOrdersRequest {
    return ListOrdersRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListOrdersRequest>): ListOrdersRequest {
    const message = createBaseListOrdersRequest();
    message.userId = object.userId ?? "";
    message.status = object.status ?? 0;
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListOrdersResponse(): ListOrdersResponse {
  return { orders: [], page: undefined };
}

export const ListOrdersResponse: MessageFns<ListOrdersResponse> = {
  encode(message: ListOrdersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.orders) {
      Order.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListOrdersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListOrdersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.orders.push(Order.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListOrdersResponse {
    return {
      orders: globalThis.Array.isArray(object?.orders) ? object.orders.map((e: any) => Order.fromJSON(e)) : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListOrdersResponse): unknown {
    const obj: any = {};
    if (message.orders?.length) {
      obj.orders = message.orders.map((e) => Order.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListOrdersResponse>): ListOrdersResponse {
    return ListOrdersResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListOrdersResponse>): ListOrdersResponse {
    const message = createBaseListOrdersResponse();
    message.orders = object.orders?.map((e) => Order.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBasePlaceOrderRequest(): PlaceOrderRequest {
  return { userId: "", symbol: "", side: 0, kind: 0, quantity: "0", price: "0", commandMetadata: undefined };
}

export const PlaceOrderRequest: MessageFns<PlaceOrderRequest> = {
  encode(message: PlaceOrderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.symbol !== "") {
      writer.uint32(18).string(message.symbol);
    }
    if (message.side !== 0) {
      writer.uint32(24).int32(message.side);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.quantity !== "0") {
      writer.uint32(40).int64(message.quantity);
    }
    if (message.price !== "0") {
      writer.uint32(48).int64(message.price);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaceOrderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaceOrderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.side = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.quantity = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.price = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaceOrderRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      side: isSet(object.side) ? orderSideFromJSON(object.side) : 0,
      kind: isSet(object.kind) ? orderKindFromJSON(object.kind) : 0,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "0",
      price: isSet(object.price) ? globalThis.String(object.price) : "0",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: PlaceOrderRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.side !== 0) {
      obj.side = orderSideToJSON(message.side);
    }
    if (message.kind !== 0) {
      obj.kind = orderKindToJSON(message.kind);
    }
    if (message.quantity !== "0") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "0") {
      obj.price = message.price;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<PlaceOrderRequest>): PlaceOrderRequest {
    return PlaceOrderRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaceOrderRequest>): PlaceOrderRequest {
    const message = createBasePlaceOrderRequest();
    message.userId = object.userId ?? "";
    message.symbol = object.symbol ?? "";
    message.side = object.side ?? 0;
    message.kind = object.kind ?? 0;
    message.quantity = object.quantity ?? "0";
    message.price = object.price ?? "0";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBasePlaceOrderResponse(): PlaceOrderResponse {
  return { order: undefined };
}

export const PlaceOrderResponse: MessageFns<PlaceOrderResponse> = {
  encode(message: PlaceOrderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.order !== undefined) {
      Order.encode(message.order, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaceOrderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaceOrderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.order = Order.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaceOrderResponse {
    return { order: isSet(object.order) ? Order.fromJSON(object.order) : undefined };
  },

  toJSON(message: PlaceOrderResponse): unknown {
    const obj: any = {};
    if (message.order !== undefined) {
      obj.order = Order.toJSON(message.order);
    }
    return obj;
  },

  create(base?: DeepPartial<PlaceOrderResponse>): PlaceOrderResponse {
    return PlaceOrderResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaceOrderResponse>): PlaceOrderResponse {
    const message = createBasePlaceOrderResponse();
    message.order = (object.order !== undefined && object.order !== null) ? Order.fromPartial(object.order) : undefined;
    return message;
  },
};

function createBaseCancelOrderRequest(): CancelOrderRequest {
  return { userId: "", orderId: "", commandMetadata: undefined };
}

export const CancelOrderRequest: MessageFns<CancelOrderRequest> = {
  encode(message: CancelOrderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.orderId !== "") {
      writer.uint32(18).string(message.orderId);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelOrderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelOrderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelOrderRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      orderId: isSet(object.orderId)
        ? globalThis.String(object.orderId)
        : isSet(object.order_id)
        ? globalThis.String(object.order_id)
        : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: CancelOrderRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.orderId !== "") {
      obj.orderId = message.orderId;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<CancelOrderRequest>): CancelOrderRequest {
    return CancelOrderRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CancelOrderRequest>): CancelOrderRequest {
    const message = createBaseCancelOrderRequest();
    message.userId = object.userId ?? "";
    message.orderId = object.orderId ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseCancelOrderResponse(): CancelOrderResponse {
  return { order: undefined, releasedAmount: "0" };
}

export const CancelOrderResponse: MessageFns<CancelOrderResponse> = {
  encode(message: CancelOrderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.order !== undefined) {
      Order.encode(message.order, writer.uint32(10).fork()).join();
    }
    if (message.releasedAmount !== "0") {
      writer.uint32(16).int64(message.releasedAmount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelOrderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelOrderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.order = Order.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.releasedAmount = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelOrderResponse {
    return {
      order: isSet(object.order) ? Order.fromJSON(object.order) : undefined,
      releasedAmount: isSet(object.releasedAmount)
        ? globalThis.String(object.releasedAmount)
        : isSet(object.released_amount)
        ? globalThis.String(object.released_amount)
        : "0",
    };
  },

  toJSON(message: CancelOrderResponse): unknown {
    const obj: any = {};
    if (message.order !== undefined) {
      obj.order = Order.toJSON(message.order);
    }
    if (message.releasedAmount !== "0") {
      obj.releasedAmount = message.releasedAmount;
    }
    return obj;
  },

  create(base?: DeepPartial<CancelOrderResponse>): CancelOrderResponse {
    return CancelOrderResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CancelOrderResponse>): CancelOrderResponse {
    const message = createBaseCancelOrderResponse();
    message.order = (object.order !== undefined && object.order !== null) ? Order.fromPartial(object.order) : undefined;
    message.releasedAmount = object.releasedAmount ?? "0";
    return message;
  },
};

function createBaseAmendOrderRequest(): AmendOrderRequest {
  return { userId: "", orderId: "", quantity: "0", price: "0", commandMetadata: undefined };
}

export const AmendOrderRequest: MessageFns<AmendOrderRequest> = {
  encode(message: AmendOrderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.orderId !== "") {
      writer.uint32(18).string(message.orderId);
    }
    if (message.quantity !== "0") {
      writer.uint32(24).int64(message.quantity);
    }
    if (message.price !== "0") {
      writer.uint32(32).int64(message.price);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AmendOrderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAmendOrderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.quantity = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.price = reader.int64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AmendOrderRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      orderId: isSet(object.orderId)
        ? globalThis.String(object.orderId)
        : isSet(object.order_id)
        ? globalThis.String(object.order_id)
        : "",
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "0",
      price: isSet(object.price) ? globalThis.String(object.price) : "0",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: AmendOrderRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.orderId !== "") {
      obj.orderId = message.orderId;
    }
    if (message.quantity !== "0") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "0") {
      obj.price = message.price;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<AmendOrderRequest>): AmendOrderRequest {
    return AmendOrderRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AmendOrderRequest>): AmendOrderRequest {
    const message = createBaseAmendOrderRequest();
    message.userId = object.userId ?? "";
    message.orderId = object.orderId ?? "";
    message.quantity = object.quantity ?? "0";
    message.price = object.price ?? "0";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseAmendOrderResponse(): AmendOrderResponse {
  return { order: undefined };
}

export const AmendOrderResponse: MessageFns<AmendOrderResponse> = {
  encode(message: AmendOrderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.order !== undefined) {
      Order.encode(message.order, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AmendOrderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAmendOrderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.order = Order.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AmendOrderResponse {
    return { order: isSet(object.order) ? Order.fromJSON(object.order) : undefined };
  },

  toJSON(message: AmendOrderResponse): unknown {
    const obj: any = {};
    if (message.order !== undefined) {
      obj.order = Order.toJSON(message.order);
    }
    return obj;
  },

  create(base?: DeepPartial<AmendOrderResponse>): AmendOrderResponse {
    return AmendOrderResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AmendOrderResponse>): AmendOrderResponse {
    const message = createBaseAmendOrderResponse();
    message.order = (object.order !== undefined && object.order !== null) ? Order.fromPartial(object.order) : undefined;
    return message;
  },
};

function createBaseListReservationsRequest(): ListReservationsRequest {
  return { userId: "", status: 0, page: undefined };
}

export const ListReservationsRequest: MessageFns<ListReservationsRequest> = {
  encode(message: ListReservationsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListReservationsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListReservationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListReservationsRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      status: isSet(object.status) ? reservationStatusFromJSON(object.status) : 0,
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListReservationsRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.status !== 0) {
      obj.status = reservationStatusToJSON(message.status);
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListReservationsRequest>): ListReservationsRequest {
    return ListReservationsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListReservationsRequest>): ListReservationsRequest {
    const message = createBaseListReservationsRequest();
    message.userId = object.userId ?? "";
    message.status = object.status ?? 0;
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListReservationsResponse(): ListReservationsResponse {
  return { reservations: [], page: undefined };
}

export const ListReservationsResponse: MessageFns<ListReservationsResponse> = {
  encode(message: ListReservationsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.reservations) {
      Reservation.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListReservationsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListReservationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservations.push(Reservation.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListReservationsResponse {
    return {
      reservations: globalThis.Array.isArray(object?.reservations)
        ? object.reservations.map((e: any) => Reservation.fromJSON(e))
        : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListReservationsResponse): unknown {
    const obj: any = {};
    if (message.reservations?.length) {
      obj.reservations = message.reservations.map((e) => Reservation.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListReservationsResponse>): ListReservationsResponse {
    return ListReservationsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListReservationsResponse>): ListReservationsResponse {
    const message = createBaseListReservationsResponse();
    message.reservations = object.reservations?.map((e) => Reservation.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBasePlaceReservationRequest(): PlaceReservationRequest {
  return {
    userId: "",
    symbol: "",
    side: 0,
    timing: 0,
    kind: 0,
    quantity: "0",
    price: "0",
    scheduledDate: "",
    commandMetadata: undefined,
  };
}

export const PlaceReservationRequest: MessageFns<PlaceReservationRequest> = {
  encode(message: PlaceReservationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.symbol !== "") {
      writer.uint32(18).string(message.symbol);
    }
    if (message.side !== 0) {
      writer.uint32(24).int32(message.side);
    }
    if (message.timing !== 0) {
      writer.uint32(32).int32(message.timing);
    }
    if (message.kind !== 0) {
      writer.uint32(40).int32(message.kind);
    }
    if (message.quantity !== "0") {
      writer.uint32(48).int64(message.quantity);
    }
    if (message.price !== "0") {
      writer.uint32(56).int64(message.price);
    }
    if (message.scheduledDate !== "") {
      writer.uint32(66).string(message.scheduledDate);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaceReservationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaceReservationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.side = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timing = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.quantity = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.price = reader.int64().toString();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.scheduledDate = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaceReservationRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      side: isSet(object.side) ? orderSideFromJSON(object.side) : 0,
      timing: isSet(object.timing) ? reservationTimingFromJSON(object.timing) : 0,
      kind: isSet(object.kind) ? orderKindFromJSON(object.kind) : 0,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "0",
      price: isSet(object.price) ? globalThis.String(object.price) : "0",
      scheduledDate: isSet(object.scheduledDate)
        ? globalThis.String(object.scheduledDate)
        : isSet(object.scheduled_date)
        ? globalThis.String(object.scheduled_date)
        : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: PlaceReservationRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.side !== 0) {
      obj.side = orderSideToJSON(message.side);
    }
    if (message.timing !== 0) {
      obj.timing = reservationTimingToJSON(message.timing);
    }
    if (message.kind !== 0) {
      obj.kind = orderKindToJSON(message.kind);
    }
    if (message.quantity !== "0") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "0") {
      obj.price = message.price;
    }
    if (message.scheduledDate !== "") {
      obj.scheduledDate = message.scheduledDate;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<PlaceReservationRequest>): PlaceReservationRequest {
    return PlaceReservationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaceReservationRequest>): PlaceReservationRequest {
    const message = createBasePlaceReservationRequest();
    message.userId = object.userId ?? "";
    message.symbol = object.symbol ?? "";
    message.side = object.side ?? 0;
    message.timing = object.timing ?? 0;
    message.kind = object.kind ?? 0;
    message.quantity = object.quantity ?? "0";
    message.price = object.price ?? "0";
    message.scheduledDate = object.scheduledDate ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBasePlaceReservationResponse(): PlaceReservationResponse {
  return { reservation: undefined };
}

export const PlaceReservationResponse: MessageFns<PlaceReservationResponse> = {
  encode(message: PlaceReservationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservation !== undefined) {
      Reservation.encode(message.reservation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaceReservationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaceReservationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservation = Reservation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaceReservationResponse {
    return { reservation: isSet(object.reservation) ? Reservation.fromJSON(object.reservation) : undefined };
  },

  toJSON(message: PlaceReservationResponse): unknown {
    const obj: any = {};
    if (message.reservation !== undefined) {
      obj.reservation = Reservation.toJSON(message.reservation);
    }
    return obj;
  },

  create(base?: DeepPartial<PlaceReservationResponse>): PlaceReservationResponse {
    return PlaceReservationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaceReservationResponse>): PlaceReservationResponse {
    const message = createBasePlaceReservationResponse();
    message.reservation = (object.reservation !== undefined && object.reservation !== null)
      ? Reservation.fromPartial(object.reservation)
      : undefined;
    return message;
  },
};

function createBaseCancelReservationRequest(): CancelReservationRequest {
  return { userId: "", reservationId: "", commandMetadata: undefined };
}

export const CancelReservationRequest: MessageFns<CancelReservationRequest> = {
  encode(message: CancelReservationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.reservationId !== "") {
      writer.uint32(18).string(message.reservationId);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelReservationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelReservationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelReservationRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      reservationId: isSet(object.reservationId)
        ? globalThis.String(object.reservationId)
        : isSet(object.reservation_id)
        ? globalThis.String(object.reservation_id)
        : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: CancelReservationRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.reservationId !== "") {
      obj.reservationId = message.reservationId;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<CancelReservationRequest>): CancelReservationRequest {
    return CancelReservationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CancelReservationRequest>): CancelReservationRequest {
    const message = createBaseCancelReservationRequest();
    message.userId = object.userId ?? "";
    message.reservationId = object.reservationId ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseCancelReservationResponse(): CancelReservationResponse {
  return { reservation: undefined, releasedAmount: "0" };
}

export const CancelReservationResponse: MessageFns<CancelReservationResponse> = {
  encode(message: CancelReservationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservation !== undefined) {
      Reservation.encode(message.reservation, writer.uint32(10).fork()).join();
    }
    if (message.releasedAmount !== "0") {
      writer.uint32(16).int64(message.releasedAmount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelReservationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelReservationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservation = Reservation.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.releasedAmount = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelReservationResponse {
    return {
      reservation: isSet(object.reservation) ? Reservation.fromJSON(object.reservation) : undefined,
      releasedAmount: isSet(object.releasedAmount)
        ? globalThis.String(object.releasedAmount)
        : isSet(object.released_amount)
        ? globalThis.String(object.released_amount)
        : "0",
    };
  },

  toJSON(message: CancelReservationResponse): unknown {
    const obj: any = {};
    if (message.reservation !== undefined) {
      obj.reservation = Reservation.toJSON(message.reservation);
    }
    if (message.releasedAmount !== "0") {
      obj.releasedAmount = message.releasedAmount;
    }
    return obj;
  },

  create(base?: DeepPartial<CancelReservationResponse>): CancelReservationResponse {
    return CancelReservationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CancelReservationResponse>): CancelReservationResponse {
    const message = createBaseCancelReservationResponse();
    message.reservation = (object.reservation !== undefined && object.reservation !== null)
      ? Reservation.fromPartial(object.reservation)
      : undefined;
    message.releasedAmount = object.releasedAmount ?? "0";
    return message;
  },
};

function createBaseAmendReservationRequest(): AmendReservationRequest {
  return {
    userId: "",
    reservationId: "",
    timing: 0,
    kind: 0,
    quantity: "0",
    price: "0",
    scheduledDate: "",
    commandMetadata: undefined,
  };
}

export const AmendReservationRequest: MessageFns<AmendReservationRequest> = {
  encode(message: AmendReservationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.reservationId !== "") {
      writer.uint32(18).string(message.reservationId);
    }
    if (message.timing !== 0) {
      writer.uint32(24).int32(message.timing);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.quantity !== "0") {
      writer.uint32(40).int64(message.quantity);
    }
    if (message.price !== "0") {
      writer.uint32(48).int64(message.price);
    }
    if (message.scheduledDate !== "") {
      writer.uint32(58).string(message.scheduledDate);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AmendReservationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAmendReservationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timing = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.quantity = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.price = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.scheduledDate = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AmendReservationRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      reservationId: isSet(object.reservationId)
        ? globalThis.String(object.reservationId)
        : isSet(object.reservation_id)
        ? globalThis.String(object.reservation_id)
        : "",
      timing: isSet(object.timing) ? reservationTimingFromJSON(object.timing) : 0,
      kind: isSet(object.kind) ? orderKindFromJSON(object.kind) : 0,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "0",
      price: isSet(object.price) ? globalThis.String(object.price) : "0",
      scheduledDate: isSet(object.scheduledDate)
        ? globalThis.String(object.scheduledDate)
        : isSet(object.scheduled_date)
        ? globalThis.String(object.scheduled_date)
        : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: AmendReservationRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.reservationId !== "") {
      obj.reservationId = message.reservationId;
    }
    if (message.timing !== 0) {
      obj.timing = reservationTimingToJSON(message.timing);
    }
    if (message.kind !== 0) {
      obj.kind = orderKindToJSON(message.kind);
    }
    if (message.quantity !== "0") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "0") {
      obj.price = message.price;
    }
    if (message.scheduledDate !== "") {
      obj.scheduledDate = message.scheduledDate;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<AmendReservationRequest>): AmendReservationRequest {
    return AmendReservationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AmendReservationRequest>): AmendReservationRequest {
    const message = createBaseAmendReservationRequest();
    message.userId = object.userId ?? "";
    message.reservationId = object.reservationId ?? "";
    message.timing = object.timing ?? 0;
    message.kind = object.kind ?? 0;
    message.quantity = object.quantity ?? "0";
    message.price = object.price ?? "0";
    message.scheduledDate = object.scheduledDate ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseAmendReservationResponse(): AmendReservationResponse {
  return { reservation: undefined };
}

export const AmendReservationResponse: MessageFns<AmendReservationResponse> = {
  encode(message: AmendReservationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservation !== undefined) {
      Reservation.encode(message.reservation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AmendReservationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAmendReservationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservation = Reservation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AmendReservationResponse {
    return { reservation: isSet(object.reservation) ? Reservation.fromJSON(object.reservation) : undefined };
  },

  toJSON(message: AmendReservationResponse): unknown {
    const obj: any = {};
    if (message.reservation !== undefined) {
      obj.reservation = Reservation.toJSON(message.reservation);
    }
    return obj;
  },

  create(base?: DeepPartial<AmendReservationResponse>): AmendReservationResponse {
    return AmendReservationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AmendReservationResponse>): AmendReservationResponse {
    const message = createBaseAmendReservationResponse();
    message.reservation = (object.reservation !== undefined && object.reservation !== null)
      ? Reservation.fromPartial(object.reservation)
      : undefined;
    return message;
  },
};

export type TradingServiceDefinition = typeof TradingServiceDefinition;
export const TradingServiceDefinition = {
  name: "TradingService",
  fullName: "candle.trading.v1.TradingService",
  methods: {
    getBalance: {
      name: "GetBalance",
      requestType: GetBalanceRequest as typeof GetBalanceRequest,
      requestStream: false,
      responseType: GetBalanceResponse as typeof GetBalanceResponse,
      responseStream: false,
      options: {},
    },
    listHoldings: {
      name: "ListHoldings",
      requestType: ListHoldingsRequest as typeof ListHoldingsRequest,
      requestStream: false,
      responseType: ListHoldingsResponse as typeof ListHoldingsResponse,
      responseStream: false,
      options: {},
    },
    listOrders: {
      name: "ListOrders",
      requestType: ListOrdersRequest as typeof ListOrdersRequest,
      requestStream: false,
      responseType: ListOrdersResponse as typeof ListOrdersResponse,
      responseStream: false,
      options: {},
    },
    placeOrder: {
      name: "PlaceOrder",
      requestType: PlaceOrderRequest as typeof PlaceOrderRequest,
      requestStream: false,
      responseType: PlaceOrderResponse as typeof PlaceOrderResponse,
      responseStream: false,
      options: {},
    },
    cancelOrder: {
      name: "CancelOrder",
      requestType: CancelOrderRequest as typeof CancelOrderRequest,
      requestStream: false,
      responseType: CancelOrderResponse as typeof CancelOrderResponse,
      responseStream: false,
      options: {},
    },
    amendOrder: {
      name: "AmendOrder",
      requestType: AmendOrderRequest as typeof AmendOrderRequest,
      requestStream: false,
      responseType: AmendOrderResponse as typeof AmendOrderResponse,
      responseStream: false,
      options: {},
    },
    listReservations: {
      name: "ListReservations",
      requestType: ListReservationsRequest as typeof ListReservationsRequest,
      requestStream: false,
      responseType: ListReservationsResponse as typeof ListReservationsResponse,
      responseStream: false,
      options: {},
    },
    placeReservation: {
      name: "PlaceReservation",
      requestType: PlaceReservationRequest as typeof PlaceReservationRequest,
      requestStream: false,
      responseType: PlaceReservationResponse as typeof PlaceReservationResponse,
      responseStream: false,
      options: {},
    },
    cancelReservation: {
      name: "CancelReservation",
      requestType: CancelReservationRequest as typeof CancelReservationRequest,
      requestStream: false,
      responseType: CancelReservationResponse as typeof CancelReservationResponse,
      responseStream: false,
      options: {},
    },
    amendReservation: {
      name: "AmendReservation",
      requestType: AmendReservationRequest as typeof AmendReservationRequest,
      requestStream: false,
      responseType: AmendReservationResponse as typeof AmendReservationResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface TradingServiceImplementation<CallContextExt = {}> {
  getBalance(
    request: GetBalanceRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetBalanceResponse>>;
  listHoldings(
    request: ListHoldingsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListHoldingsResponse>>;
  listOrders(
    request: ListOrdersRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListOrdersResponse>>;
  placeOrder(
    request: PlaceOrderRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<PlaceOrderResponse>>;
  cancelOrder(
    request: CancelOrderRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<CancelOrderResponse>>;
  amendOrder(
    request: AmendOrderRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<AmendOrderResponse>>;
  listReservations(
    request: ListReservationsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListReservationsResponse>>;
  placeReservation(
    request: PlaceReservationRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<PlaceReservationResponse>>;
  cancelReservation(
    request: CancelReservationRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<CancelReservationResponse>>;
  amendReservation(
    request: AmendReservationRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<AmendReservationResponse>>;
}

export interface TradingServiceClient<CallOptionsExt = {}> {
  getBalance(
    request: DeepPartial<GetBalanceRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetBalanceResponse>;
  listHoldings(
    request: DeepPartial<ListHoldingsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListHoldingsResponse>;
  listOrders(
    request: DeepPartial<ListOrdersRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListOrdersResponse>;
  placeOrder(
    request: DeepPartial<PlaceOrderRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<PlaceOrderResponse>;
  cancelOrder(
    request: DeepPartial<CancelOrderRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<CancelOrderResponse>;
  amendOrder(
    request: DeepPartial<AmendOrderRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<AmendOrderResponse>;
  listReservations(
    request: DeepPartial<ListReservationsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListReservationsResponse>;
  placeReservation(
    request: DeepPartial<PlaceReservationRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<PlaceReservationResponse>;
  cancelReservation(
    request: DeepPartial<CancelReservationRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<CancelReservationResponse>;
  amendReservation(
    request: DeepPartial<AmendReservationRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<AmendReservationResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
