// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: candle/trading/v1/reservation.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { CommandMetadata, PageRequest, PageResponse } from "../../common/v1/common";
import {
  OrderKind,
  orderKindFromJSON,
  orderKindToJSON,
  OrderSide,
  orderSideFromJSON,
  orderSideToJSON,
  ReservationStatus,
  reservationStatusFromJSON,
  reservationStatusToJSON,
  ReservationTiming,
  reservationTimingFromJSON,
  reservationTimingToJSON,
} from "./trading_common";

export const protobufPackage = "candle.trading.v1";

export interface Reservation {
  id: string;
  userId: string;
  symbol: string;
  side: OrderSide;
  timing: ReservationTiming;
  kind: OrderKind;
  quantity: string;
  price: string;
  scheduledDate: string;
  reservedAmount: string;
  status: ReservationStatus;
  parentOrderId: string;
  createdAt?: Date | undefined;
}

export interface ListReservationsRequest {
  userId: string;
  status: ReservationStatus;
  page?: PageRequest | undefined;
}

export interface ListReservationsResponse {
  reservations: Reservation[];
  page?: PageResponse | undefined;
}

export interface PlaceReservationRequest {
  userId: string;
  symbol: string;
  side: OrderSide;
  timing: ReservationTiming;
  kind: OrderKind;
  quantity: string;
  price: string;
  scheduledDate: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface PlaceReservationResponse {
  reservation?: Reservation | undefined;
}

export interface CancelReservationRequest {
  userId: string;
  reservationId: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface CancelReservationResponse {
  reservation?: Reservation | undefined;
  releasedAmount: string;
}

/** 정정 = 원예약 취소 후 parent_order_id를 가진 새 예약 생성(CAN-006~008). */
export interface AmendReservationRequest {
  userId: string;
  reservationId: string;
  timing: ReservationTiming;
  kind: OrderKind;
  quantity: string;
  price: string;
  scheduledDate: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface AmendReservationResponse {
  reservation?: Reservation | undefined;
}

function createBaseReservation(): Reservation {
  return {
    id: "",
    userId: "",
    symbol: "",
    side: 0,
    timing: 0,
    kind: 0,
    quantity: "0",
    price: "0",
    scheduledDate: "",
    reservedAmount: "0",
    status: 0,
    parentOrderId: "",
    createdAt: undefined,
  };
}

export const Reservation: MessageFns<Reservation> = {
  encode(message: Reservation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.symbol !== "") {
      writer.uint32(26).string(message.symbol);
    }
    if (message.side !== 0) {
      writer.uint32(32).int32(message.side);
    }
    if (message.timing !== 0) {
      writer.uint32(40).int32(message.timing);
    }
    if (message.kind !== 0) {
      writer.uint32(48).int32(message.kind);
    }
    if (message.quantity !== "0") {
      writer.uint32(56).int64(message.quantity);
    }
    if (message.price !== "0") {
      writer.uint32(64).int64(message.price);
    }
    if (message.scheduledDate !== "") {
      writer.uint32(74).string(message.scheduledDate);
    }
    if (message.reservedAmount !== "0") {
      writer.uint32(80).int64(message.reservedAmount);
    }
    if (message.status !== 0) {
      writer.uint32(88).int32(message.status);
    }
    if (message.parentOrderId !== "") {
      writer.uint32(98).string(message.parentOrderId);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Reservation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReservation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.side = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.timing = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.quantity = reader.int64().toString();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.price = reader.int64().toString();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.scheduledDate = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.reservedAmount = reader.int64().toString();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.parentOrderId = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Reservation {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      side: isSet(object.side) ? orderSideFromJSON(object.side) : 0,
      timing: isSet(object.timing) ? reservationTimingFromJSON(object.timing) : 0,
      kind: isSet(object.kind) ? orderKindFromJSON(object.kind) : 0,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "0",
      price: isSet(object.price) ? globalThis.String(object.price) : "0",
      scheduledDate: isSet(object.scheduledDate)
        ? globalThis.String(object.scheduledDate)
        : isSet(object.scheduled_date)
        ? globalThis.String(object.scheduled_date)
        : "",
      reservedAmount: isSet(object.reservedAmount)
        ? globalThis.String(object.reservedAmount)
        : isSet(object.reserved_amount)
        ? globalThis.String(object.reserved_amount)
        : "0",
      status: isSet(object.status) ? reservationStatusFromJSON(object.status) : 0,
      parentOrderId: isSet(object.parentOrderId)
        ? globalThis.String(object.parentOrderId)
        : isSet(object.parent_order_id)
        ? globalThis.String(object.parent_order_id)
        : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: Reservation): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.side !== 0) {
      obj.side = orderSideToJSON(message.side);
    }
    if (message.timing !== 0) {
      obj.timing = reservationTimingToJSON(message.timing);
    }
    if (message.kind !== 0) {
      obj.kind = orderKindToJSON(message.kind);
    }
    if (message.quantity !== "0") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "0") {
      obj.price = message.price;
    }
    if (message.scheduledDate !== "") {
      obj.scheduledDate = message.scheduledDate;
    }
    if (message.reservedAmount !== "0") {
      obj.reservedAmount = message.reservedAmount;
    }
    if (message.status !== 0) {
      obj.status = reservationStatusToJSON(message.status);
    }
    if (message.parentOrderId !== "") {
      obj.parentOrderId = message.parentOrderId;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Reservation>): Reservation {
    return Reservation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Reservation>): Reservation {
    const message = createBaseReservation();
    message.id = object.id ?? "";
    message.userId = object.userId ?? "";
    message.symbol = object.symbol ?? "";
    message.side = object.side ?? 0;
    message.timing = object.timing ?? 0;
    message.kind = object.kind ?? 0;
    message.quantity = object.quantity ?? "0";
    message.price = object.price ?? "0";
    message.scheduledDate = object.scheduledDate ?? "";
    message.reservedAmount = object.reservedAmount ?? "0";
    message.status = object.status ?? 0;
    message.parentOrderId = object.parentOrderId ?? "";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseListReservationsRequest(): ListReservationsRequest {
  return { userId: "", status: 0, page: undefined };
}

export const ListReservationsRequest: MessageFns<ListReservationsRequest> = {
  encode(message: ListReservationsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListReservationsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListReservationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListReservationsRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      status: isSet(object.status) ? reservationStatusFromJSON(object.status) : 0,
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListReservationsRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.status !== 0) {
      obj.status = reservationStatusToJSON(message.status);
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListReservationsRequest>): ListReservationsRequest {
    return ListReservationsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListReservationsRequest>): ListReservationsRequest {
    const message = createBaseListReservationsRequest();
    message.userId = object.userId ?? "";
    message.status = object.status ?? 0;
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListReservationsResponse(): ListReservationsResponse {
  return { reservations: [], page: undefined };
}

export const ListReservationsResponse: MessageFns<ListReservationsResponse> = {
  encode(message: ListReservationsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.reservations) {
      Reservation.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListReservationsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListReservationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservations.push(Reservation.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListReservationsResponse {
    return {
      reservations: globalThis.Array.isArray(object?.reservations)
        ? object.reservations.map((e: any) => Reservation.fromJSON(e))
        : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListReservationsResponse): unknown {
    const obj: any = {};
    if (message.reservations?.length) {
      obj.reservations = message.reservations.map((e) => Reservation.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListReservationsResponse>): ListReservationsResponse {
    return ListReservationsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListReservationsResponse>): ListReservationsResponse {
    const message = createBaseListReservationsResponse();
    message.reservations = object.reservations?.map((e) => Reservation.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBasePlaceReservationRequest(): PlaceReservationRequest {
  return {
    userId: "",
    symbol: "",
    side: 0,
    timing: 0,
    kind: 0,
    quantity: "0",
    price: "0",
    scheduledDate: "",
    commandMetadata: undefined,
  };
}

export const PlaceReservationRequest: MessageFns<PlaceReservationRequest> = {
  encode(message: PlaceReservationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.symbol !== "") {
      writer.uint32(18).string(message.symbol);
    }
    if (message.side !== 0) {
      writer.uint32(24).int32(message.side);
    }
    if (message.timing !== 0) {
      writer.uint32(32).int32(message.timing);
    }
    if (message.kind !== 0) {
      writer.uint32(40).int32(message.kind);
    }
    if (message.quantity !== "0") {
      writer.uint32(48).int64(message.quantity);
    }
    if (message.price !== "0") {
      writer.uint32(56).int64(message.price);
    }
    if (message.scheduledDate !== "") {
      writer.uint32(66).string(message.scheduledDate);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaceReservationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaceReservationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.side = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timing = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.quantity = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.price = reader.int64().toString();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.scheduledDate = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaceReservationRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      side: isSet(object.side) ? orderSideFromJSON(object.side) : 0,
      timing: isSet(object.timing) ? reservationTimingFromJSON(object.timing) : 0,
      kind: isSet(object.kind) ? orderKindFromJSON(object.kind) : 0,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "0",
      price: isSet(object.price) ? globalThis.String(object.price) : "0",
      scheduledDate: isSet(object.scheduledDate)
        ? globalThis.String(object.scheduledDate)
        : isSet(object.scheduled_date)
        ? globalThis.String(object.scheduled_date)
        : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: PlaceReservationRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.side !== 0) {
      obj.side = orderSideToJSON(message.side);
    }
    if (message.timing !== 0) {
      obj.timing = reservationTimingToJSON(message.timing);
    }
    if (message.kind !== 0) {
      obj.kind = orderKindToJSON(message.kind);
    }
    if (message.quantity !== "0") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "0") {
      obj.price = message.price;
    }
    if (message.scheduledDate !== "") {
      obj.scheduledDate = message.scheduledDate;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<PlaceReservationRequest>): PlaceReservationRequest {
    return PlaceReservationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaceReservationRequest>): PlaceReservationRequest {
    const message = createBasePlaceReservationRequest();
    message.userId = object.userId ?? "";
    message.symbol = object.symbol ?? "";
    message.side = object.side ?? 0;
    message.timing = object.timing ?? 0;
    message.kind = object.kind ?? 0;
    message.quantity = object.quantity ?? "0";
    message.price = object.price ?? "0";
    message.scheduledDate = object.scheduledDate ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBasePlaceReservationResponse(): PlaceReservationResponse {
  return { reservation: undefined };
}

export const PlaceReservationResponse: MessageFns<PlaceReservationResponse> = {
  encode(message: PlaceReservationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservation !== undefined) {
      Reservation.encode(message.reservation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaceReservationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaceReservationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservation = Reservation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaceReservationResponse {
    return { reservation: isSet(object.reservation) ? Reservation.fromJSON(object.reservation) : undefined };
  },

  toJSON(message: PlaceReservationResponse): unknown {
    const obj: any = {};
    if (message.reservation !== undefined) {
      obj.reservation = Reservation.toJSON(message.reservation);
    }
    return obj;
  },

  create(base?: DeepPartial<PlaceReservationResponse>): PlaceReservationResponse {
    return PlaceReservationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaceReservationResponse>): PlaceReservationResponse {
    const message = createBasePlaceReservationResponse();
    message.reservation = (object.reservation !== undefined && object.reservation !== null)
      ? Reservation.fromPartial(object.reservation)
      : undefined;
    return message;
  },
};

function createBaseCancelReservationRequest(): CancelReservationRequest {
  return { userId: "", reservationId: "", commandMetadata: undefined };
}

export const CancelReservationRequest: MessageFns<CancelReservationRequest> = {
  encode(message: CancelReservationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.reservationId !== "") {
      writer.uint32(18).string(message.reservationId);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelReservationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelReservationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelReservationRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      reservationId: isSet(object.reservationId)
        ? globalThis.String(object.reservationId)
        : isSet(object.reservation_id)
        ? globalThis.String(object.reservation_id)
        : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: CancelReservationRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.reservationId !== "") {
      obj.reservationId = message.reservationId;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<CancelReservationRequest>): CancelReservationRequest {
    return CancelReservationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CancelReservationRequest>): CancelReservationRequest {
    const message = createBaseCancelReservationRequest();
    message.userId = object.userId ?? "";
    message.reservationId = object.reservationId ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseCancelReservationResponse(): CancelReservationResponse {
  return { reservation: undefined, releasedAmount: "0" };
}

export const CancelReservationResponse: MessageFns<CancelReservationResponse> = {
  encode(message: CancelReservationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservation !== undefined) {
      Reservation.encode(message.reservation, writer.uint32(10).fork()).join();
    }
    if (message.releasedAmount !== "0") {
      writer.uint32(16).int64(message.releasedAmount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CancelReservationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelReservationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservation = Reservation.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.releasedAmount = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelReservationResponse {
    return {
      reservation: isSet(object.reservation) ? Reservation.fromJSON(object.reservation) : undefined,
      releasedAmount: isSet(object.releasedAmount)
        ? globalThis.String(object.releasedAmount)
        : isSet(object.released_amount)
        ? globalThis.String(object.released_amount)
        : "0",
    };
  },

  toJSON(message: CancelReservationResponse): unknown {
    const obj: any = {};
    if (message.reservation !== undefined) {
      obj.reservation = Reservation.toJSON(message.reservation);
    }
    if (message.releasedAmount !== "0") {
      obj.releasedAmount = message.releasedAmount;
    }
    return obj;
  },

  create(base?: DeepPartial<CancelReservationResponse>): CancelReservationResponse {
    return CancelReservationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CancelReservationResponse>): CancelReservationResponse {
    const message = createBaseCancelReservationResponse();
    message.reservation = (object.reservation !== undefined && object.reservation !== null)
      ? Reservation.fromPartial(object.reservation)
      : undefined;
    message.releasedAmount = object.releasedAmount ?? "0";
    return message;
  },
};

function createBaseAmendReservationRequest(): AmendReservationRequest {
  return {
    userId: "",
    reservationId: "",
    timing: 0,
    kind: 0,
    quantity: "0",
    price: "0",
    scheduledDate: "",
    commandMetadata: undefined,
  };
}

export const AmendReservationRequest: MessageFns<AmendReservationRequest> = {
  encode(message: AmendReservationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.reservationId !== "") {
      writer.uint32(18).string(message.reservationId);
    }
    if (message.timing !== 0) {
      writer.uint32(24).int32(message.timing);
    }
    if (message.kind !== 0) {
      writer.uint32(32).int32(message.kind);
    }
    if (message.quantity !== "0") {
      writer.uint32(40).int64(message.quantity);
    }
    if (message.price !== "0") {
      writer.uint32(48).int64(message.price);
    }
    if (message.scheduledDate !== "") {
      writer.uint32(58).string(message.scheduledDate);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AmendReservationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAmendReservationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timing = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.quantity = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.price = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.scheduledDate = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AmendReservationRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      reservationId: isSet(object.reservationId)
        ? globalThis.String(object.reservationId)
        : isSet(object.reservation_id)
        ? globalThis.String(object.reservation_id)
        : "",
      timing: isSet(object.timing) ? reservationTimingFromJSON(object.timing) : 0,
      kind: isSet(object.kind) ? orderKindFromJSON(object.kind) : 0,
      quantity: isSet(object.quantity) ? globalThis.String(object.quantity) : "0",
      price: isSet(object.price) ? globalThis.String(object.price) : "0",
      scheduledDate: isSet(object.scheduledDate)
        ? globalThis.String(object.scheduledDate)
        : isSet(object.scheduled_date)
        ? globalThis.String(object.scheduled_date)
        : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: AmendReservationRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.reservationId !== "") {
      obj.reservationId = message.reservationId;
    }
    if (message.timing !== 0) {
      obj.timing = reservationTimingToJSON(message.timing);
    }
    if (message.kind !== 0) {
      obj.kind = orderKindToJSON(message.kind);
    }
    if (message.quantity !== "0") {
      obj.quantity = message.quantity;
    }
    if (message.price !== "0") {
      obj.price = message.price;
    }
    if (message.scheduledDate !== "") {
      obj.scheduledDate = message.scheduledDate;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<AmendReservationRequest>): AmendReservationRequest {
    return AmendReservationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AmendReservationRequest>): AmendReservationRequest {
    const message = createBaseAmendReservationRequest();
    message.userId = object.userId ?? "";
    message.reservationId = object.reservationId ?? "";
    message.timing = object.timing ?? 0;
    message.kind = object.kind ?? 0;
    message.quantity = object.quantity ?? "0";
    message.price = object.price ?? "0";
    message.scheduledDate = object.scheduledDate ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseAmendReservationResponse(): AmendReservationResponse {
  return { reservation: undefined };
}

export const AmendReservationResponse: MessageFns<AmendReservationResponse> = {
  encode(message: AmendReservationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservation !== undefined) {
      Reservation.encode(message.reservation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AmendReservationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAmendReservationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservation = Reservation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AmendReservationResponse {
    return { reservation: isSet(object.reservation) ? Reservation.fromJSON(object.reservation) : undefined };
  },

  toJSON(message: AmendReservationResponse): unknown {
    const obj: any = {};
    if (message.reservation !== undefined) {
      obj.reservation = Reservation.toJSON(message.reservation);
    }
    return obj;
  },

  create(base?: DeepPartial<AmendReservationResponse>): AmendReservationResponse {
    return AmendReservationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AmendReservationResponse>): AmendReservationResponse {
    const message = createBaseAmendReservationResponse();
    message.reservation = (object.reservation !== undefined && object.reservation !== null)
      ? Reservation.fromPartial(object.reservation)
      : undefined;
    return message;
  },
};

export type ReservationServiceDefinition = typeof ReservationServiceDefinition;
export const ReservationServiceDefinition = {
  name: "ReservationService",
  fullName: "candle.trading.v1.ReservationService",
  methods: {
    listReservations: {
      name: "ListReservations",
      requestType: ListReservationsRequest as typeof ListReservationsRequest,
      requestStream: false,
      responseType: ListReservationsResponse as typeof ListReservationsResponse,
      responseStream: false,
      options: {},
    },
    placeReservation: {
      name: "PlaceReservation",
      requestType: PlaceReservationRequest as typeof PlaceReservationRequest,
      requestStream: false,
      responseType: PlaceReservationResponse as typeof PlaceReservationResponse,
      responseStream: false,
      options: {},
    },
    cancelReservation: {
      name: "CancelReservation",
      requestType: CancelReservationRequest as typeof CancelReservationRequest,
      requestStream: false,
      responseType: CancelReservationResponse as typeof CancelReservationResponse,
      responseStream: false,
      options: {},
    },
    amendReservation: {
      name: "AmendReservation",
      requestType: AmendReservationRequest as typeof AmendReservationRequest,
      requestStream: false,
      responseType: AmendReservationResponse as typeof AmendReservationResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface ReservationServiceImplementation<CallContextExt = {}> {
  listReservations(
    request: ListReservationsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListReservationsResponse>>;
  placeReservation(
    request: PlaceReservationRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<PlaceReservationResponse>>;
  cancelReservation(
    request: CancelReservationRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<CancelReservationResponse>>;
  amendReservation(
    request: AmendReservationRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<AmendReservationResponse>>;
}

export interface ReservationServiceClient<CallOptionsExt = {}> {
  listReservations(
    request: DeepPartial<ListReservationsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListReservationsResponse>;
  placeReservation(
    request: DeepPartial<PlaceReservationRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<PlaceReservationResponse>;
  cancelReservation(
    request: DeepPartial<CancelReservationRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<CancelReservationResponse>;
  amendReservation(
    request: DeepPartial<AmendReservationRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<AmendReservationResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
