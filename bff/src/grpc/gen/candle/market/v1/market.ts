// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: candle/market/v1/market.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { PageRequest, PageResponse } from "../../common/v1/common";

export const protobufPackage = "candle.market.v1";

export interface Stock {
  symbol: string;
  name: string;
  market: string;
}

export interface Quote {
  symbol: string;
  price: string;
  change: string;
  quotedAt?: Date | undefined;
}

export interface SearchStocksRequest {
  query: string;
  market: string;
  page?: PageRequest | undefined;
}

export interface SearchStocksResponse {
  stocks: Stock[];
  page?: PageResponse | undefined;
}

export interface GetQuoteRequest {
  symbol: string;
}

export interface GetQuoteResponse {
  quote?: Quote | undefined;
}

export interface BatchQuotesRequest {
  symbols: string[];
}

export interface BatchQuotesResponse {
  quotes: Quote[];
}

function createBaseStock(): Stock {
  return { symbol: "", name: "", market: "" };
}

export const Stock: MessageFns<Stock> = {
  encode(message: Stock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.symbol !== "") {
      writer.uint32(10).string(message.symbol);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.market !== "") {
      writer.uint32(26).string(message.market);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Stock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStock();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.market = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Stock {
    return {
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      market: isSet(object.market) ? globalThis.String(object.market) : "",
    };
  },

  toJSON(message: Stock): unknown {
    const obj: any = {};
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.market !== "") {
      obj.market = message.market;
    }
    return obj;
  },

  create(base?: DeepPartial<Stock>): Stock {
    return Stock.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Stock>): Stock {
    const message = createBaseStock();
    message.symbol = object.symbol ?? "";
    message.name = object.name ?? "";
    message.market = object.market ?? "";
    return message;
  },
};

function createBaseQuote(): Quote {
  return { symbol: "", price: "0", change: "0", quotedAt: undefined };
}

export const Quote: MessageFns<Quote> = {
  encode(message: Quote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.symbol !== "") {
      writer.uint32(10).string(message.symbol);
    }
    if (message.price !== "0") {
      writer.uint32(16).int64(message.price);
    }
    if (message.change !== "0") {
      writer.uint32(24).int64(message.change);
    }
    if (message.quotedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.quotedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Quote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.price = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.change = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.quotedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Quote {
    return {
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      price: isSet(object.price) ? globalThis.String(object.price) : "0",
      change: isSet(object.change) ? globalThis.String(object.change) : "0",
      quotedAt: isSet(object.quotedAt)
        ? fromJsonTimestamp(object.quotedAt)
        : isSet(object.quoted_at)
        ? fromJsonTimestamp(object.quoted_at)
        : undefined,
    };
  },

  toJSON(message: Quote): unknown {
    const obj: any = {};
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.price !== "0") {
      obj.price = message.price;
    }
    if (message.change !== "0") {
      obj.change = message.change;
    }
    if (message.quotedAt !== undefined) {
      obj.quotedAt = message.quotedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Quote>): Quote {
    return Quote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Quote>): Quote {
    const message = createBaseQuote();
    message.symbol = object.symbol ?? "";
    message.price = object.price ?? "0";
    message.change = object.change ?? "0";
    message.quotedAt = object.quotedAt ?? undefined;
    return message;
  },
};

function createBaseSearchStocksRequest(): SearchStocksRequest {
  return { query: "", market: "", page: undefined };
}

export const SearchStocksRequest: MessageFns<SearchStocksRequest> = {
  encode(message: SearchStocksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.market !== "") {
      writer.uint32(18).string(message.market);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchStocksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchStocksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.market = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchStocksRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      market: isSet(object.market) ? globalThis.String(object.market) : "",
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: SearchStocksRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.market !== "") {
      obj.market = message.market;
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchStocksRequest>): SearchStocksRequest {
    return SearchStocksRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchStocksRequest>): SearchStocksRequest {
    const message = createBaseSearchStocksRequest();
    message.query = object.query ?? "";
    message.market = object.market ?? "";
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseSearchStocksResponse(): SearchStocksResponse {
  return { stocks: [], page: undefined };
}

export const SearchStocksResponse: MessageFns<SearchStocksResponse> = {
  encode(message: SearchStocksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.stocks) {
      Stock.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchStocksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchStocksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stocks.push(Stock.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchStocksResponse {
    return {
      stocks: globalThis.Array.isArray(object?.stocks) ? object.stocks.map((e: any) => Stock.fromJSON(e)) : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: SearchStocksResponse): unknown {
    const obj: any = {};
    if (message.stocks?.length) {
      obj.stocks = message.stocks.map((e) => Stock.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchStocksResponse>): SearchStocksResponse {
    return SearchStocksResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchStocksResponse>): SearchStocksResponse {
    const message = createBaseSearchStocksResponse();
    message.stocks = object.stocks?.map((e) => Stock.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseGetQuoteRequest(): GetQuoteRequest {
  return { symbol: "" };
}

export const GetQuoteRequest: MessageFns<GetQuoteRequest> = {
  encode(message: GetQuoteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.symbol !== "") {
      writer.uint32(10).string(message.symbol);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetQuoteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetQuoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetQuoteRequest {
    return { symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "" };
  },

  toJSON(message: GetQuoteRequest): unknown {
    const obj: any = {};
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    return obj;
  },

  create(base?: DeepPartial<GetQuoteRequest>): GetQuoteRequest {
    return GetQuoteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetQuoteRequest>): GetQuoteRequest {
    const message = createBaseGetQuoteRequest();
    message.symbol = object.symbol ?? "";
    return message;
  },
};

function createBaseGetQuoteResponse(): GetQuoteResponse {
  return { quote: undefined };
}

export const GetQuoteResponse: MessageFns<GetQuoteResponse> = {
  encode(message: GetQuoteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.quote !== undefined) {
      Quote.encode(message.quote, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetQuoteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetQuoteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quote = Quote.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetQuoteResponse {
    return { quote: isSet(object.quote) ? Quote.fromJSON(object.quote) : undefined };
  },

  toJSON(message: GetQuoteResponse): unknown {
    const obj: any = {};
    if (message.quote !== undefined) {
      obj.quote = Quote.toJSON(message.quote);
    }
    return obj;
  },

  create(base?: DeepPartial<GetQuoteResponse>): GetQuoteResponse {
    return GetQuoteResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetQuoteResponse>): GetQuoteResponse {
    const message = createBaseGetQuoteResponse();
    message.quote = (object.quote !== undefined && object.quote !== null) ? Quote.fromPartial(object.quote) : undefined;
    return message;
  },
};

function createBaseBatchQuotesRequest(): BatchQuotesRequest {
  return { symbols: [] };
}

export const BatchQuotesRequest: MessageFns<BatchQuotesRequest> = {
  encode(message: BatchQuotesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.symbols) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchQuotesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchQuotesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.symbols.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchQuotesRequest {
    return {
      symbols: globalThis.Array.isArray(object?.symbols) ? object.symbols.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: BatchQuotesRequest): unknown {
    const obj: any = {};
    if (message.symbols?.length) {
      obj.symbols = message.symbols;
    }
    return obj;
  },

  create(base?: DeepPartial<BatchQuotesRequest>): BatchQuotesRequest {
    return BatchQuotesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BatchQuotesRequest>): BatchQuotesRequest {
    const message = createBaseBatchQuotesRequest();
    message.symbols = object.symbols?.map((e) => e) || [];
    return message;
  },
};

function createBaseBatchQuotesResponse(): BatchQuotesResponse {
  return { quotes: [] };
}

export const BatchQuotesResponse: MessageFns<BatchQuotesResponse> = {
  encode(message: BatchQuotesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.quotes) {
      Quote.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchQuotesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchQuotesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quotes.push(Quote.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchQuotesResponse {
    return { quotes: globalThis.Array.isArray(object?.quotes) ? object.quotes.map((e: any) => Quote.fromJSON(e)) : [] };
  },

  toJSON(message: BatchQuotesResponse): unknown {
    const obj: any = {};
    if (message.quotes?.length) {
      obj.quotes = message.quotes.map((e) => Quote.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<BatchQuotesResponse>): BatchQuotesResponse {
    return BatchQuotesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BatchQuotesResponse>): BatchQuotesResponse {
    const message = createBaseBatchQuotesResponse();
    message.quotes = object.quotes?.map((e) => Quote.fromPartial(e)) || [];
    return message;
  },
};

export type MarketServiceDefinition = typeof MarketServiceDefinition;
export const MarketServiceDefinition = {
  name: "MarketService",
  fullName: "candle.market.v1.MarketService",
  methods: {
    searchStocks: {
      name: "SearchStocks",
      requestType: SearchStocksRequest as typeof SearchStocksRequest,
      requestStream: false,
      responseType: SearchStocksResponse as typeof SearchStocksResponse,
      responseStream: false,
      options: {},
    },
    getQuote: {
      name: "GetQuote",
      requestType: GetQuoteRequest as typeof GetQuoteRequest,
      requestStream: false,
      responseType: GetQuoteResponse as typeof GetQuoteResponse,
      responseStream: false,
      options: {},
    },
    batchQuotes: {
      name: "BatchQuotes",
      requestType: BatchQuotesRequest as typeof BatchQuotesRequest,
      requestStream: false,
      responseType: BatchQuotesResponse as typeof BatchQuotesResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface MarketServiceImplementation<CallContextExt = {}> {
  searchStocks(
    request: SearchStocksRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<SearchStocksResponse>>;
  getQuote(request: GetQuoteRequest, context: CallContext & CallContextExt): Promise<DeepPartial<GetQuoteResponse>>;
  batchQuotes(
    request: BatchQuotesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<BatchQuotesResponse>>;
}

export interface MarketServiceClient<CallOptionsExt = {}> {
  searchStocks(
    request: DeepPartial<SearchStocksRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<SearchStocksResponse>;
  getQuote(request: DeepPartial<GetQuoteRequest>, options?: CallOptions & CallOptionsExt): Promise<GetQuoteResponse>;
  batchQuotes(
    request: DeepPartial<BatchQuotesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<BatchQuotesResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
