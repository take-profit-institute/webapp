// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: candle/news/v1/news.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "candle.news.v1";

export interface GetStockNewsRequest {
  stockCode: string;
}

export interface GetStockNewsResponse {
  articles: NewsArticle[];
}

export interface NewsArticle {
  id: string;
  title: string;
  contentSummary: string;
  url: string;
  source: string;
  publishedAt?: Date | undefined;
  createdAt?: Date | undefined;
}

function createBaseGetStockNewsRequest(): GetStockNewsRequest {
  return { stockCode: "" };
}

export const GetStockNewsRequest: MessageFns<GetStockNewsRequest> = {
  encode(message: GetStockNewsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stockCode !== "") {
      writer.uint32(10).string(message.stockCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStockNewsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStockNewsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stockCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStockNewsRequest {
    return {
      stockCode: isSet(object.stockCode)
        ? globalThis.String(object.stockCode)
        : isSet(object.stock_code)
        ? globalThis.String(object.stock_code)
        : "",
    };
  },

  toJSON(message: GetStockNewsRequest): unknown {
    const obj: any = {};
    if (message.stockCode !== "") {
      obj.stockCode = message.stockCode;
    }
    return obj;
  },

  create(base?: DeepPartial<GetStockNewsRequest>): GetStockNewsRequest {
    return GetStockNewsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetStockNewsRequest>): GetStockNewsRequest {
    const message = createBaseGetStockNewsRequest();
    message.stockCode = object.stockCode ?? "";
    return message;
  },
};

function createBaseGetStockNewsResponse(): GetStockNewsResponse {
  return { articles: [] };
}

export const GetStockNewsResponse: MessageFns<GetStockNewsResponse> = {
  encode(message: GetStockNewsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.articles) {
      NewsArticle.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStockNewsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStockNewsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.articles.push(NewsArticle.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStockNewsResponse {
    return {
      articles: globalThis.Array.isArray(object?.articles)
        ? object.articles.map((e: any) => NewsArticle.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetStockNewsResponse): unknown {
    const obj: any = {};
    if (message.articles?.length) {
      obj.articles = message.articles.map((e) => NewsArticle.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetStockNewsResponse>): GetStockNewsResponse {
    return GetStockNewsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetStockNewsResponse>): GetStockNewsResponse {
    const message = createBaseGetStockNewsResponse();
    message.articles = object.articles?.map((e) => NewsArticle.fromPartial(e)) || [];
    return message;
  },
};

function createBaseNewsArticle(): NewsArticle {
  return { id: "", title: "", contentSummary: "", url: "", source: "", publishedAt: undefined, createdAt: undefined };
}

export const NewsArticle: MessageFns<NewsArticle> = {
  encode(message: NewsArticle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.contentSummary !== "") {
      writer.uint32(26).string(message.contentSummary);
    }
    if (message.url !== "") {
      writer.uint32(34).string(message.url);
    }
    if (message.source !== "") {
      writer.uint32(42).string(message.source);
    }
    if (message.publishedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.publishedAt), writer.uint32(50).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NewsArticle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNewsArticle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.contentSummary = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.publishedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NewsArticle {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      contentSummary: isSet(object.contentSummary)
        ? globalThis.String(object.contentSummary)
        : isSet(object.content_summary)
        ? globalThis.String(object.content_summary)
        : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      publishedAt: isSet(object.publishedAt)
        ? fromJsonTimestamp(object.publishedAt)
        : isSet(object.published_at)
        ? fromJsonTimestamp(object.published_at)
        : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: NewsArticle): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.contentSummary !== "") {
      obj.contentSummary = message.contentSummary;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.publishedAt !== undefined) {
      obj.publishedAt = message.publishedAt.toISOString();
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<NewsArticle>): NewsArticle {
    return NewsArticle.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NewsArticle>): NewsArticle {
    const message = createBaseNewsArticle();
    message.id = object.id ?? "";
    message.title = object.title ?? "";
    message.contentSummary = object.contentSummary ?? "";
    message.url = object.url ?? "";
    message.source = object.source ?? "";
    message.publishedAt = object.publishedAt ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

export type NewsServiceDefinition = typeof NewsServiceDefinition;
export const NewsServiceDefinition = {
  name: "NewsService",
  fullName: "candle.news.v1.NewsService",
  methods: {
    getStockNews: {
      name: "GetStockNews",
      requestType: GetStockNewsRequest as typeof GetStockNewsRequest,
      requestStream: false,
      responseType: GetStockNewsResponse as typeof GetStockNewsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface NewsServiceImplementation<CallContextExt = {}> {
  getStockNews(
    request: GetStockNewsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetStockNewsResponse>>;
}

export interface NewsServiceClient<CallOptionsExt = {}> {
  getStockNews(
    request: DeepPartial<GetStockNewsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetStockNewsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
