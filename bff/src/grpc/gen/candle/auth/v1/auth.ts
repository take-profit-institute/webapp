// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
// source: candle/auth/v1/auth.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";

export const protobufPackage = "candle.auth.v1";

export interface Provider {
  name: string;
  authorizationUrl: string;
}

export interface ListProvidersRequest {}

export interface ListProvidersResponse {
  providers: Provider[];
}

function createBaseProvider(): Provider {
  return { name: "", authorizationUrl: "" };
}

export const Provider: MessageFns<Provider> = {
  encode(message: Provider, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") writer.uint32(10).string(message.name);
    if (message.authorizationUrl !== "") writer.uint32(18).string(message.authorizationUrl);
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Provider {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProvider();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: if (tag === 10) { message.name = reader.string(); continue; } break;
        case 2: if (tag === 18) { message.authorizationUrl = reader.string(); continue; } break;
      }
      if ((tag & 7) === 4 || tag === 0) break;
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Provider {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      authorizationUrl: isSet(object.authorizationUrl)
        ? globalThis.String(object.authorizationUrl)
        : isSet(object.authorization_url)
        ? globalThis.String(object.authorization_url)
        : "",
    };
  },

  toJSON(message: Provider): unknown {
    const obj: any = {};
    if (message.name !== "") obj.name = message.name;
    if (message.authorizationUrl !== "") obj.authorizationUrl = message.authorizationUrl;
    return obj;
  },

  create(base?: DeepPartial<Provider>): Provider { return Provider.fromPartial(base ?? {}); },
  fromPartial(object: DeepPartial<Provider>): Provider {
    const message = createBaseProvider();
    message.name = object.name ?? "";
    message.authorizationUrl = object.authorizationUrl ?? "";
    return message;
  },
};

function createBaseListProvidersRequest(): ListProvidersRequest {
  return {};
}

export const ListProvidersRequest: MessageFns<ListProvidersRequest> = {
  encode(_message: ListProvidersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter { return writer; },
  decode(input: BinaryReader | Uint8Array, length?: number): ListProvidersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) break;
      reader.skip(tag & 7);
    }
    return createBaseListProvidersRequest();
  },
  fromJSON(_object: any): ListProvidersRequest { return {}; },
  toJSON(_message: ListProvidersRequest): unknown { return {}; },
  create(_base?: DeepPartial<ListProvidersRequest>): ListProvidersRequest { return {}; },
  fromPartial(_object: DeepPartial<ListProvidersRequest>): ListProvidersRequest { return {}; },
};

function createBaseListProvidersResponse(): ListProvidersResponse {
  return { providers: [] };
}

export const ListProvidersResponse: MessageFns<ListProvidersResponse> = {
  encode(message: ListProvidersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.providers) {
      Provider.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProvidersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProvidersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: if (tag === 10) { message.providers.push(Provider.decode(reader, reader.uint32())); continue; } break;
      }
      if ((tag & 7) === 4 || tag === 0) break;
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProvidersResponse {
    return {
      providers: globalThis.Array.isArray(object?.providers)
        ? object.providers.map((e: any) => Provider.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListProvidersResponse): unknown {
    const obj: any = {};
    if (message.providers?.length) obj.providers = message.providers.map((v) => Provider.toJSON(v));
    return obj;
  },

  create(base?: DeepPartial<ListProvidersResponse>): ListProvidersResponse {
    return ListProvidersResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListProvidersResponse>): ListProvidersResponse {
    const message = createBaseListProvidersResponse();
    message.providers = object.providers?.map((e) => Provider.fromPartial(e)) ?? [];
    return message;
  },
};

export type AuthServiceDefinition = typeof AuthServiceDefinition;
export const AuthServiceDefinition = {
  name: "AuthService",
  fullName: "candle.auth.v1.AuthService",
  methods: {
    listProviders: {
      name: "ListProviders",
      requestType: ListProvidersRequest as typeof ListProvidersRequest,
      requestStream: false,
      responseType: ListProvidersResponse as typeof ListProvidersResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface AuthServiceImplementation<CallContextExt = {}> {
  listProviders(request: ListProvidersRequest, context: CallContext & CallContextExt): Promise<DeepPartial<ListProvidersResponse>>;
}

export interface AuthServiceClient<CallOptionsExt = {}> {
  listProviders(request: DeepPartial<ListProvidersRequest>, options?: CallOptions & CallOptionsExt): Promise<ListProvidersResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
