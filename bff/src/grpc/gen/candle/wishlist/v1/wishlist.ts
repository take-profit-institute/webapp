// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: candle/wishlist/v1/wishlist.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { CommandMetadata, PageRequest, PageResponse } from "../../common/v1/common";

export const protobufPackage = "candle.wishlist.v1";

export interface AddWishlistItemRequest {
  userId: string;
  symbol: string;
  displayName: string;
  market: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface AddWishlistItemResponse {
  item?: WishlistItem | undefined;
}

export interface RemoveWishlistItemRequest {
  userId: string;
  symbol: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface RemoveWishlistItemResponse {
}

export interface ListWishlistItemsRequest {
  userId: string;
  pageRequest?: PageRequest | undefined;
}

export interface ListWishlistItemsResponse {
  items: WishlistItem[];
  pageResponse?: PageResponse | undefined;
}

export interface WishlistItem {
  id: string;
  userId: string;
  symbol: string;
  displayName: string;
  market: string;
  createdAt?: Date | undefined;
}

function createBaseAddWishlistItemRequest(): AddWishlistItemRequest {
  return { userId: "", symbol: "", displayName: "", market: "", commandMetadata: undefined };
}

export const AddWishlistItemRequest: MessageFns<AddWishlistItemRequest> = {
  encode(message: AddWishlistItemRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.symbol !== "") {
      writer.uint32(18).string(message.symbol);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    if (message.market !== "") {
      writer.uint32(34).string(message.market);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddWishlistItemRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddWishlistItemRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.market = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddWishlistItemRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      market: isSet(object.market) ? globalThis.String(object.market) : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: AddWishlistItemRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.market !== "") {
      obj.market = message.market;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<AddWishlistItemRequest>): AddWishlistItemRequest {
    return AddWishlistItemRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AddWishlistItemRequest>): AddWishlistItemRequest {
    const message = createBaseAddWishlistItemRequest();
    message.userId = object.userId ?? "";
    message.symbol = object.symbol ?? "";
    message.displayName = object.displayName ?? "";
    message.market = object.market ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseAddWishlistItemResponse(): AddWishlistItemResponse {
  return { item: undefined };
}

export const AddWishlistItemResponse: MessageFns<AddWishlistItemResponse> = {
  encode(message: AddWishlistItemResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.item !== undefined) {
      WishlistItem.encode(message.item, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddWishlistItemResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddWishlistItemResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.item = WishlistItem.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddWishlistItemResponse {
    return { item: isSet(object.item) ? WishlistItem.fromJSON(object.item) : undefined };
  },

  toJSON(message: AddWishlistItemResponse): unknown {
    const obj: any = {};
    if (message.item !== undefined) {
      obj.item = WishlistItem.toJSON(message.item);
    }
    return obj;
  },

  create(base?: DeepPartial<AddWishlistItemResponse>): AddWishlistItemResponse {
    return AddWishlistItemResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AddWishlistItemResponse>): AddWishlistItemResponse {
    const message = createBaseAddWishlistItemResponse();
    message.item = (object.item !== undefined && object.item !== null)
      ? WishlistItem.fromPartial(object.item)
      : undefined;
    return message;
  },
};

function createBaseRemoveWishlistItemRequest(): RemoveWishlistItemRequest {
  return { userId: "", symbol: "", commandMetadata: undefined };
}

export const RemoveWishlistItemRequest: MessageFns<RemoveWishlistItemRequest> = {
  encode(message: RemoveWishlistItemRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.symbol !== "") {
      writer.uint32(18).string(message.symbol);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveWishlistItemRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveWishlistItemRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveWishlistItemRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: RemoveWishlistItemRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<RemoveWishlistItemRequest>): RemoveWishlistItemRequest {
    return RemoveWishlistItemRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RemoveWishlistItemRequest>): RemoveWishlistItemRequest {
    const message = createBaseRemoveWishlistItemRequest();
    message.userId = object.userId ?? "";
    message.symbol = object.symbol ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseRemoveWishlistItemResponse(): RemoveWishlistItemResponse {
  return {};
}

export const RemoveWishlistItemResponse: MessageFns<RemoveWishlistItemResponse> = {
  encode(_: RemoveWishlistItemResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveWishlistItemResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveWishlistItemResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RemoveWishlistItemResponse {
    return {};
  },

  toJSON(_: RemoveWishlistItemResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<RemoveWishlistItemResponse>): RemoveWishlistItemResponse {
    return RemoveWishlistItemResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RemoveWishlistItemResponse>): RemoveWishlistItemResponse {
    const message = createBaseRemoveWishlistItemResponse();
    return message;
  },
};

function createBaseListWishlistItemsRequest(): ListWishlistItemsRequest {
  return { userId: "", pageRequest: undefined };
}

export const ListWishlistItemsRequest: MessageFns<ListWishlistItemsRequest> = {
  encode(message: ListWishlistItemsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.pageRequest !== undefined) {
      PageRequest.encode(message.pageRequest, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListWishlistItemsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListWishlistItemsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pageRequest = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListWishlistItemsRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      pageRequest: isSet(object.pageRequest)
        ? PageRequest.fromJSON(object.pageRequest)
        : isSet(object.page_request)
        ? PageRequest.fromJSON(object.page_request)
        : undefined,
    };
  },

  toJSON(message: ListWishlistItemsRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.pageRequest !== undefined) {
      obj.pageRequest = PageRequest.toJSON(message.pageRequest);
    }
    return obj;
  },

  create(base?: DeepPartial<ListWishlistItemsRequest>): ListWishlistItemsRequest {
    return ListWishlistItemsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListWishlistItemsRequest>): ListWishlistItemsRequest {
    const message = createBaseListWishlistItemsRequest();
    message.userId = object.userId ?? "";
    message.pageRequest = (object.pageRequest !== undefined && object.pageRequest !== null)
      ? PageRequest.fromPartial(object.pageRequest)
      : undefined;
    return message;
  },
};

function createBaseListWishlistItemsResponse(): ListWishlistItemsResponse {
  return { items: [], pageResponse: undefined };
}

export const ListWishlistItemsResponse: MessageFns<ListWishlistItemsResponse> = {
  encode(message: ListWishlistItemsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      WishlistItem.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pageResponse !== undefined) {
      PageResponse.encode(message.pageResponse, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListWishlistItemsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListWishlistItemsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(WishlistItem.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pageResponse = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListWishlistItemsResponse {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => WishlistItem.fromJSON(e)) : [],
      pageResponse: isSet(object.pageResponse)
        ? PageResponse.fromJSON(object.pageResponse)
        : isSet(object.page_response)
        ? PageResponse.fromJSON(object.page_response)
        : undefined,
    };
  },

  toJSON(message: ListWishlistItemsResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => WishlistItem.toJSON(e));
    }
    if (message.pageResponse !== undefined) {
      obj.pageResponse = PageResponse.toJSON(message.pageResponse);
    }
    return obj;
  },

  create(base?: DeepPartial<ListWishlistItemsResponse>): ListWishlistItemsResponse {
    return ListWishlistItemsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListWishlistItemsResponse>): ListWishlistItemsResponse {
    const message = createBaseListWishlistItemsResponse();
    message.items = object.items?.map((e) => WishlistItem.fromPartial(e)) || [];
    message.pageResponse = (object.pageResponse !== undefined && object.pageResponse !== null)
      ? PageResponse.fromPartial(object.pageResponse)
      : undefined;
    return message;
  },
};

function createBaseWishlistItem(): WishlistItem {
  return { id: "", userId: "", symbol: "", displayName: "", market: "", createdAt: undefined };
}

export const WishlistItem: MessageFns<WishlistItem> = {
  encode(message: WishlistItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.symbol !== "") {
      writer.uint32(26).string(message.symbol);
    }
    if (message.displayName !== "") {
      writer.uint32(34).string(message.displayName);
    }
    if (message.market !== "") {
      writer.uint32(42).string(message.market);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WishlistItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWishlistItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.symbol = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.market = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WishlistItem {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      symbol: isSet(object.symbol) ? globalThis.String(object.symbol) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      market: isSet(object.market) ? globalThis.String(object.market) : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: WishlistItem): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.symbol !== "") {
      obj.symbol = message.symbol;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.market !== "") {
      obj.market = message.market;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<WishlistItem>): WishlistItem {
    return WishlistItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WishlistItem>): WishlistItem {
    const message = createBaseWishlistItem();
    message.id = object.id ?? "";
    message.userId = object.userId ?? "";
    message.symbol = object.symbol ?? "";
    message.displayName = object.displayName ?? "";
    message.market = object.market ?? "";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

export type WishlistServiceDefinition = typeof WishlistServiceDefinition;
export const WishlistServiceDefinition = {
  name: "WishlistService",
  fullName: "candle.wishlist.v1.WishlistService",
  methods: {
    addWishlistItem: {
      name: "AddWishlistItem",
      requestType: AddWishlistItemRequest as typeof AddWishlistItemRequest,
      requestStream: false,
      responseType: AddWishlistItemResponse as typeof AddWishlistItemResponse,
      responseStream: false,
      options: {},
    },
    removeWishlistItem: {
      name: "RemoveWishlistItem",
      requestType: RemoveWishlistItemRequest as typeof RemoveWishlistItemRequest,
      requestStream: false,
      responseType: RemoveWishlistItemResponse as typeof RemoveWishlistItemResponse,
      responseStream: false,
      options: {},
    },
    listWishlistItems: {
      name: "ListWishlistItems",
      requestType: ListWishlistItemsRequest as typeof ListWishlistItemsRequest,
      requestStream: false,
      responseType: ListWishlistItemsResponse as typeof ListWishlistItemsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface WishlistServiceImplementation<CallContextExt = {}> {
  addWishlistItem(
    request: AddWishlistItemRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<AddWishlistItemResponse>>;
  removeWishlistItem(
    request: RemoveWishlistItemRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<RemoveWishlistItemResponse>>;
  listWishlistItems(
    request: ListWishlistItemsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListWishlistItemsResponse>>;
}

export interface WishlistServiceClient<CallOptionsExt = {}> {
  addWishlistItem(
    request: DeepPartial<AddWishlistItemRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<AddWishlistItemResponse>;
  removeWishlistItem(
    request: DeepPartial<RemoveWishlistItemRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<RemoveWishlistItemResponse>;
  listWishlistItems(
    request: DeepPartial<ListWishlistItemsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListWishlistItemsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
