// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: candle/ranking/v1/ranking.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { CommandMetadata, PageRequest, PageResponse } from "../../common/v1/common";

export const protobufPackage = "candle.ranking.v1";

export interface RankingEntry {
  rank: string;
  userId: string;
  nickname: string;
  returnRate: string;
  totalAsset: string;
  tradeCount: number;
  rankingDate: string;
}

export interface FinalizeDailyRankingRequest {
  rankingDate: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface FinalizeDailyRankingResponse {
  rankingDate: string;
  rankedUserCount: number;
}

export interface ListRankingsRequest {
  /** 서버 기본값은 20, 최대값은 100이다. */
  page?: PageRequest | undefined;
}

export interface ListRankingsResponse {
  rankings: RankingEntry[];
  page?: PageResponse | undefined;
}

export interface GetMyRankingRequest {
  /** 인증 metadata의 x-user-id와 일치해야 한다. */
  userId: string;
}

export interface GetMyRankingResponse {
  ranking?: RankingEntry | undefined;
}

function createBaseRankingEntry(): RankingEntry {
  return { rank: "0", userId: "", nickname: "", returnRate: "", totalAsset: "0", tradeCount: 0, rankingDate: "" };
}

export const RankingEntry: MessageFns<RankingEntry> = {
  encode(message: RankingEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rank !== "0") {
      writer.uint32(8).int64(message.rank);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.nickname !== "") {
      writer.uint32(26).string(message.nickname);
    }
    if (message.returnRate !== "") {
      writer.uint32(34).string(message.returnRate);
    }
    if (message.totalAsset !== "0") {
      writer.uint32(40).int64(message.totalAsset);
    }
    if (message.tradeCount !== 0) {
      writer.uint32(48).int32(message.tradeCount);
    }
    if (message.rankingDate !== "") {
      writer.uint32(58).string(message.rankingDate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RankingEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRankingEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.rank = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nickname = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.returnRate = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.totalAsset = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.tradeCount = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.rankingDate = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RankingEntry {
    return {
      rank: isSet(object.rank) ? globalThis.String(object.rank) : "0",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      nickname: isSet(object.nickname) ? globalThis.String(object.nickname) : "",
      returnRate: isSet(object.returnRate)
        ? globalThis.String(object.returnRate)
        : isSet(object.return_rate)
        ? globalThis.String(object.return_rate)
        : "",
      totalAsset: isSet(object.totalAsset)
        ? globalThis.String(object.totalAsset)
        : isSet(object.total_asset)
        ? globalThis.String(object.total_asset)
        : "0",
      tradeCount: isSet(object.tradeCount)
        ? globalThis.Number(object.tradeCount)
        : isSet(object.trade_count)
        ? globalThis.Number(object.trade_count)
        : 0,
      rankingDate: isSet(object.rankingDate)
        ? globalThis.String(object.rankingDate)
        : isSet(object.ranking_date)
        ? globalThis.String(object.ranking_date)
        : "",
    };
  },

  toJSON(message: RankingEntry): unknown {
    const obj: any = {};
    if (message.rank !== "0") {
      obj.rank = message.rank;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.nickname !== "") {
      obj.nickname = message.nickname;
    }
    if (message.returnRate !== "") {
      obj.returnRate = message.returnRate;
    }
    if (message.totalAsset !== "0") {
      obj.totalAsset = message.totalAsset;
    }
    if (message.tradeCount !== 0) {
      obj.tradeCount = Math.round(message.tradeCount);
    }
    if (message.rankingDate !== "") {
      obj.rankingDate = message.rankingDate;
    }
    return obj;
  },

  create(base?: DeepPartial<RankingEntry>): RankingEntry {
    return RankingEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RankingEntry>): RankingEntry {
    const message = createBaseRankingEntry();
    message.rank = object.rank ?? "0";
    message.userId = object.userId ?? "";
    message.nickname = object.nickname ?? "";
    message.returnRate = object.returnRate ?? "";
    message.totalAsset = object.totalAsset ?? "0";
    message.tradeCount = object.tradeCount ?? 0;
    message.rankingDate = object.rankingDate ?? "";
    return message;
  },
};

function createBaseFinalizeDailyRankingRequest(): FinalizeDailyRankingRequest {
  return { rankingDate: "", commandMetadata: undefined };
}

export const FinalizeDailyRankingRequest: MessageFns<FinalizeDailyRankingRequest> = {
  encode(message: FinalizeDailyRankingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rankingDate !== "") {
      writer.uint32(10).string(message.rankingDate);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FinalizeDailyRankingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFinalizeDailyRankingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rankingDate = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FinalizeDailyRankingRequest {
    return {
      rankingDate: isSet(object.rankingDate)
        ? globalThis.String(object.rankingDate)
        : isSet(object.ranking_date)
        ? globalThis.String(object.ranking_date)
        : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: FinalizeDailyRankingRequest): unknown {
    const obj: any = {};
    if (message.rankingDate !== "") {
      obj.rankingDate = message.rankingDate;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<FinalizeDailyRankingRequest>): FinalizeDailyRankingRequest {
    return FinalizeDailyRankingRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FinalizeDailyRankingRequest>): FinalizeDailyRankingRequest {
    const message = createBaseFinalizeDailyRankingRequest();
    message.rankingDate = object.rankingDate ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseFinalizeDailyRankingResponse(): FinalizeDailyRankingResponse {
  return { rankingDate: "", rankedUserCount: 0 };
}

export const FinalizeDailyRankingResponse: MessageFns<FinalizeDailyRankingResponse> = {
  encode(message: FinalizeDailyRankingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rankingDate !== "") {
      writer.uint32(10).string(message.rankingDate);
    }
    if (message.rankedUserCount !== 0) {
      writer.uint32(16).int32(message.rankedUserCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FinalizeDailyRankingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFinalizeDailyRankingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rankingDate = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rankedUserCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FinalizeDailyRankingResponse {
    return {
      rankingDate: isSet(object.rankingDate)
        ? globalThis.String(object.rankingDate)
        : isSet(object.ranking_date)
        ? globalThis.String(object.ranking_date)
        : "",
      rankedUserCount: isSet(object.rankedUserCount)
        ? globalThis.Number(object.rankedUserCount)
        : isSet(object.ranked_user_count)
        ? globalThis.Number(object.ranked_user_count)
        : 0,
    };
  },

  toJSON(message: FinalizeDailyRankingResponse): unknown {
    const obj: any = {};
    if (message.rankingDate !== "") {
      obj.rankingDate = message.rankingDate;
    }
    if (message.rankedUserCount !== 0) {
      obj.rankedUserCount = Math.round(message.rankedUserCount);
    }
    return obj;
  },

  create(base?: DeepPartial<FinalizeDailyRankingResponse>): FinalizeDailyRankingResponse {
    return FinalizeDailyRankingResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FinalizeDailyRankingResponse>): FinalizeDailyRankingResponse {
    const message = createBaseFinalizeDailyRankingResponse();
    message.rankingDate = object.rankingDate ?? "";
    message.rankedUserCount = object.rankedUserCount ?? 0;
    return message;
  },
};

function createBaseListRankingsRequest(): ListRankingsRequest {
  return { page: undefined };
}

export const ListRankingsRequest: MessageFns<ListRankingsRequest> = {
  encode(message: ListRankingsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRankingsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRankingsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRankingsRequest {
    return { page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined };
  },

  toJSON(message: ListRankingsRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListRankingsRequest>): ListRankingsRequest {
    return ListRankingsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListRankingsRequest>): ListRankingsRequest {
    const message = createBaseListRankingsRequest();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListRankingsResponse(): ListRankingsResponse {
  return { rankings: [], page: undefined };
}

export const ListRankingsResponse: MessageFns<ListRankingsResponse> = {
  encode(message: ListRankingsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.rankings) {
      RankingEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRankingsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRankingsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rankings.push(RankingEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRankingsResponse {
    return {
      rankings: globalThis.Array.isArray(object?.rankings)
        ? object.rankings.map((e: any) => RankingEntry.fromJSON(e))
        : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListRankingsResponse): unknown {
    const obj: any = {};
    if (message.rankings?.length) {
      obj.rankings = message.rankings.map((e) => RankingEntry.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListRankingsResponse>): ListRankingsResponse {
    return ListRankingsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListRankingsResponse>): ListRankingsResponse {
    const message = createBaseListRankingsResponse();
    message.rankings = object.rankings?.map((e) => RankingEntry.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseGetMyRankingRequest(): GetMyRankingRequest {
  return { userId: "" };
}

export const GetMyRankingRequest: MessageFns<GetMyRankingRequest> = {
  encode(message: GetMyRankingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMyRankingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMyRankingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMyRankingRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
    };
  },

  toJSON(message: GetMyRankingRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetMyRankingRequest>): GetMyRankingRequest {
    return GetMyRankingRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetMyRankingRequest>): GetMyRankingRequest {
    const message = createBaseGetMyRankingRequest();
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetMyRankingResponse(): GetMyRankingResponse {
  return { ranking: undefined };
}

export const GetMyRankingResponse: MessageFns<GetMyRankingResponse> = {
  encode(message: GetMyRankingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ranking !== undefined) {
      RankingEntry.encode(message.ranking, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMyRankingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMyRankingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ranking = RankingEntry.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMyRankingResponse {
    return { ranking: isSet(object.ranking) ? RankingEntry.fromJSON(object.ranking) : undefined };
  },

  toJSON(message: GetMyRankingResponse): unknown {
    const obj: any = {};
    if (message.ranking !== undefined) {
      obj.ranking = RankingEntry.toJSON(message.ranking);
    }
    return obj;
  },

  create(base?: DeepPartial<GetMyRankingResponse>): GetMyRankingResponse {
    return GetMyRankingResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetMyRankingResponse>): GetMyRankingResponse {
    const message = createBaseGetMyRankingResponse();
    message.ranking = (object.ranking !== undefined && object.ranking !== null)
      ? RankingEntry.fromPartial(object.ranking)
      : undefined;
    return message;
  },
};

export type RankingServiceDefinition = typeof RankingServiceDefinition;
export const RankingServiceDefinition = {
  name: "RankingService",
  fullName: "candle.ranking.v1.RankingService",
  methods: {
    finalizeDailyRanking: {
      name: "FinalizeDailyRanking",
      requestType: FinalizeDailyRankingRequest as typeof FinalizeDailyRankingRequest,
      requestStream: false,
      responseType: FinalizeDailyRankingResponse as typeof FinalizeDailyRankingResponse,
      responseStream: false,
      options: {},
    },
    listRankings: {
      name: "ListRankings",
      requestType: ListRankingsRequest as typeof ListRankingsRequest,
      requestStream: false,
      responseType: ListRankingsResponse as typeof ListRankingsResponse,
      responseStream: false,
      options: {},
    },
    getMyRanking: {
      name: "GetMyRanking",
      requestType: GetMyRankingRequest as typeof GetMyRankingRequest,
      requestStream: false,
      responseType: GetMyRankingResponse as typeof GetMyRankingResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface RankingServiceImplementation<CallContextExt = {}> {
  finalizeDailyRanking(
    request: FinalizeDailyRankingRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<FinalizeDailyRankingResponse>>;
  listRankings(
    request: ListRankingsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListRankingsResponse>>;
  getMyRanking(
    request: GetMyRankingRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetMyRankingResponse>>;
}

export interface RankingServiceClient<CallOptionsExt = {}> {
  finalizeDailyRanking(
    request: DeepPartial<FinalizeDailyRankingRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<FinalizeDailyRankingResponse>;
  listRankings(
    request: DeepPartial<ListRankingsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListRankingsResponse>;
  getMyRanking(
    request: DeepPartial<GetMyRankingRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetMyRankingResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
