// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: candle/mission/v1/mission.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { CommandMetadata, PageRequest, PageResponse } from "../../common/v1/common";

export const protobufPackage = "candle.mission.v1";

export interface Mission {
  id: string;
  title: string;
  description: string;
  rewardPoints: string;
}

export interface UserMissionState {
  missionId: string;
  joined: boolean;
  completed: boolean;
  claimed: boolean;
  progress: string;
}

export interface ListMissionsRequest {
  userId: string;
  page?: PageRequest | undefined;
}

export interface ListMissionsResponse {
  missions: Mission[];
  page?: PageResponse | undefined;
}

export interface JoinMissionRequest {
  userId: string;
  missionId: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface JoinMissionResponse {
  state?: UserMissionState | undefined;
}

export interface ClaimRewardRequest {
  userId: string;
  missionId: string;
  commandMetadata?: CommandMetadata | undefined;
}

export interface ClaimRewardResponse {
  state?: UserMissionState | undefined;
  rewardedPoints: string;
}

function createBaseMission(): Mission {
  return { id: "", title: "", description: "", rewardPoints: "0" };
}

export const Mission: MessageFns<Mission> = {
  encode(message: Mission, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.rewardPoints !== "0") {
      writer.uint32(32).int64(message.rewardPoints);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Mission {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMission();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.rewardPoints = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Mission {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      rewardPoints: isSet(object.rewardPoints)
        ? globalThis.String(object.rewardPoints)
        : isSet(object.reward_points)
        ? globalThis.String(object.reward_points)
        : "0",
    };
  },

  toJSON(message: Mission): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.rewardPoints !== "0") {
      obj.rewardPoints = message.rewardPoints;
    }
    return obj;
  },

  create(base?: DeepPartial<Mission>): Mission {
    return Mission.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Mission>): Mission {
    const message = createBaseMission();
    message.id = object.id ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.rewardPoints = object.rewardPoints ?? "0";
    return message;
  },
};

function createBaseUserMissionState(): UserMissionState {
  return { missionId: "", joined: false, completed: false, claimed: false, progress: "0" };
}

export const UserMissionState: MessageFns<UserMissionState> = {
  encode(message: UserMissionState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.missionId !== "") {
      writer.uint32(10).string(message.missionId);
    }
    if (message.joined !== false) {
      writer.uint32(16).bool(message.joined);
    }
    if (message.completed !== false) {
      writer.uint32(24).bool(message.completed);
    }
    if (message.claimed !== false) {
      writer.uint32(32).bool(message.claimed);
    }
    if (message.progress !== "0") {
      writer.uint32(40).int64(message.progress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserMissionState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserMissionState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.missionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.joined = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.completed = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.claimed = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.progress = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserMissionState {
    return {
      missionId: isSet(object.missionId)
        ? globalThis.String(object.missionId)
        : isSet(object.mission_id)
        ? globalThis.String(object.mission_id)
        : "",
      joined: isSet(object.joined) ? globalThis.Boolean(object.joined) : false,
      completed: isSet(object.completed) ? globalThis.Boolean(object.completed) : false,
      claimed: isSet(object.claimed) ? globalThis.Boolean(object.claimed) : false,
      progress: isSet(object.progress) ? globalThis.String(object.progress) : "0",
    };
  },

  toJSON(message: UserMissionState): unknown {
    const obj: any = {};
    if (message.missionId !== "") {
      obj.missionId = message.missionId;
    }
    if (message.joined !== false) {
      obj.joined = message.joined;
    }
    if (message.completed !== false) {
      obj.completed = message.completed;
    }
    if (message.claimed !== false) {
      obj.claimed = message.claimed;
    }
    if (message.progress !== "0") {
      obj.progress = message.progress;
    }
    return obj;
  },

  create(base?: DeepPartial<UserMissionState>): UserMissionState {
    return UserMissionState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UserMissionState>): UserMissionState {
    const message = createBaseUserMissionState();
    message.missionId = object.missionId ?? "";
    message.joined = object.joined ?? false;
    message.completed = object.completed ?? false;
    message.claimed = object.claimed ?? false;
    message.progress = object.progress ?? "0";
    return message;
  },
};

function createBaseListMissionsRequest(): ListMissionsRequest {
  return { userId: "", page: undefined };
}

export const ListMissionsRequest: MessageFns<ListMissionsRequest> = {
  encode(message: ListMissionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListMissionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListMissionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListMissionsRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListMissionsRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListMissionsRequest>): ListMissionsRequest {
    return ListMissionsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListMissionsRequest>): ListMissionsRequest {
    const message = createBaseListMissionsRequest();
    message.userId = object.userId ?? "";
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListMissionsResponse(): ListMissionsResponse {
  return { missions: [], page: undefined };
}

export const ListMissionsResponse: MessageFns<ListMissionsResponse> = {
  encode(message: ListMissionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.missions) {
      Mission.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageResponse.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListMissionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListMissionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.missions.push(Mission.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListMissionsResponse {
    return {
      missions: globalThis.Array.isArray(object?.missions) ? object.missions.map((e: any) => Mission.fromJSON(e)) : [],
      page: isSet(object.page) ? PageResponse.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListMissionsResponse): unknown {
    const obj: any = {};
    if (message.missions?.length) {
      obj.missions = message.missions.map((e) => Mission.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageResponse.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ListMissionsResponse>): ListMissionsResponse {
    return ListMissionsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListMissionsResponse>): ListMissionsResponse {
    const message = createBaseListMissionsResponse();
    message.missions = object.missions?.map((e) => Mission.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResponse.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseJoinMissionRequest(): JoinMissionRequest {
  return { userId: "", missionId: "", commandMetadata: undefined };
}

export const JoinMissionRequest: MessageFns<JoinMissionRequest> = {
  encode(message: JoinMissionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.missionId !== "") {
      writer.uint32(18).string(message.missionId);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinMissionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinMissionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.missionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinMissionRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      missionId: isSet(object.missionId)
        ? globalThis.String(object.missionId)
        : isSet(object.mission_id)
        ? globalThis.String(object.mission_id)
        : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: JoinMissionRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.missionId !== "") {
      obj.missionId = message.missionId;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<JoinMissionRequest>): JoinMissionRequest {
    return JoinMissionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JoinMissionRequest>): JoinMissionRequest {
    const message = createBaseJoinMissionRequest();
    message.userId = object.userId ?? "";
    message.missionId = object.missionId ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseJoinMissionResponse(): JoinMissionResponse {
  return { state: undefined };
}

export const JoinMissionResponse: MessageFns<JoinMissionResponse> = {
  encode(message: JoinMissionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== undefined) {
      UserMissionState.encode(message.state, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinMissionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinMissionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.state = UserMissionState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinMissionResponse {
    return { state: isSet(object.state) ? UserMissionState.fromJSON(object.state) : undefined };
  },

  toJSON(message: JoinMissionResponse): unknown {
    const obj: any = {};
    if (message.state !== undefined) {
      obj.state = UserMissionState.toJSON(message.state);
    }
    return obj;
  },

  create(base?: DeepPartial<JoinMissionResponse>): JoinMissionResponse {
    return JoinMissionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JoinMissionResponse>): JoinMissionResponse {
    const message = createBaseJoinMissionResponse();
    message.state = (object.state !== undefined && object.state !== null)
      ? UserMissionState.fromPartial(object.state)
      : undefined;
    return message;
  },
};

function createBaseClaimRewardRequest(): ClaimRewardRequest {
  return { userId: "", missionId: "", commandMetadata: undefined };
}

export const ClaimRewardRequest: MessageFns<ClaimRewardRequest> = {
  encode(message: ClaimRewardRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.missionId !== "") {
      writer.uint32(18).string(message.missionId);
    }
    if (message.commandMetadata !== undefined) {
      CommandMetadata.encode(message.commandMetadata, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClaimRewardRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClaimRewardRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.missionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.commandMetadata = CommandMetadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClaimRewardRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      missionId: isSet(object.missionId)
        ? globalThis.String(object.missionId)
        : isSet(object.mission_id)
        ? globalThis.String(object.mission_id)
        : "",
      commandMetadata: isSet(object.commandMetadata)
        ? CommandMetadata.fromJSON(object.commandMetadata)
        : isSet(object.command_metadata)
        ? CommandMetadata.fromJSON(object.command_metadata)
        : undefined,
    };
  },

  toJSON(message: ClaimRewardRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.missionId !== "") {
      obj.missionId = message.missionId;
    }
    if (message.commandMetadata !== undefined) {
      obj.commandMetadata = CommandMetadata.toJSON(message.commandMetadata);
    }
    return obj;
  },

  create(base?: DeepPartial<ClaimRewardRequest>): ClaimRewardRequest {
    return ClaimRewardRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClaimRewardRequest>): ClaimRewardRequest {
    const message = createBaseClaimRewardRequest();
    message.userId = object.userId ?? "";
    message.missionId = object.missionId ?? "";
    message.commandMetadata = (object.commandMetadata !== undefined && object.commandMetadata !== null)
      ? CommandMetadata.fromPartial(object.commandMetadata)
      : undefined;
    return message;
  },
};

function createBaseClaimRewardResponse(): ClaimRewardResponse {
  return { state: undefined, rewardedPoints: "0" };
}

export const ClaimRewardResponse: MessageFns<ClaimRewardResponse> = {
  encode(message: ClaimRewardResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== undefined) {
      UserMissionState.encode(message.state, writer.uint32(10).fork()).join();
    }
    if (message.rewardedPoints !== "0") {
      writer.uint32(16).int64(message.rewardedPoints);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClaimRewardResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClaimRewardResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.state = UserMissionState.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rewardedPoints = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClaimRewardResponse {
    return {
      state: isSet(object.state) ? UserMissionState.fromJSON(object.state) : undefined,
      rewardedPoints: isSet(object.rewardedPoints)
        ? globalThis.String(object.rewardedPoints)
        : isSet(object.rewarded_points)
        ? globalThis.String(object.rewarded_points)
        : "0",
    };
  },

  toJSON(message: ClaimRewardResponse): unknown {
    const obj: any = {};
    if (message.state !== undefined) {
      obj.state = UserMissionState.toJSON(message.state);
    }
    if (message.rewardedPoints !== "0") {
      obj.rewardedPoints = message.rewardedPoints;
    }
    return obj;
  },

  create(base?: DeepPartial<ClaimRewardResponse>): ClaimRewardResponse {
    return ClaimRewardResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClaimRewardResponse>): ClaimRewardResponse {
    const message = createBaseClaimRewardResponse();
    message.state = (object.state !== undefined && object.state !== null)
      ? UserMissionState.fromPartial(object.state)
      : undefined;
    message.rewardedPoints = object.rewardedPoints ?? "0";
    return message;
  },
};

export type MissionServiceDefinition = typeof MissionServiceDefinition;
export const MissionServiceDefinition = {
  name: "MissionService",
  fullName: "candle.mission.v1.MissionService",
  methods: {
    listMissions: {
      name: "ListMissions",
      requestType: ListMissionsRequest as typeof ListMissionsRequest,
      requestStream: false,
      responseType: ListMissionsResponse as typeof ListMissionsResponse,
      responseStream: false,
      options: {},
    },
    joinMission: {
      name: "JoinMission",
      requestType: JoinMissionRequest as typeof JoinMissionRequest,
      requestStream: false,
      responseType: JoinMissionResponse as typeof JoinMissionResponse,
      responseStream: false,
      options: {},
    },
    claimReward: {
      name: "ClaimReward",
      requestType: ClaimRewardRequest as typeof ClaimRewardRequest,
      requestStream: false,
      responseType: ClaimRewardResponse as typeof ClaimRewardResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface MissionServiceImplementation<CallContextExt = {}> {
  listMissions(
    request: ListMissionsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ListMissionsResponse>>;
  joinMission(
    request: JoinMissionRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<JoinMissionResponse>>;
  claimReward(
    request: ClaimRewardRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ClaimRewardResponse>>;
}

export interface MissionServiceClient<CallOptionsExt = {}> {
  listMissions(
    request: DeepPartial<ListMissionsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ListMissionsResponse>;
  joinMission(
    request: DeepPartial<JoinMissionRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<JoinMissionResponse>;
  claimReward(
    request: DeepPartial<ClaimRewardRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ClaimRewardResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
