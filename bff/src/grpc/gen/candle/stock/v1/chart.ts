// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: candle/stock/v1/chart.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "candle.stock.v1";

export enum CandleInterval {
  CANDLE_INTERVAL_UNSPECIFIED = 0,
  MIN_1 = 1,
  MIN_5 = 2,
  DAY_1 = 3,
  WEEK_1 = 4,
  MONTH_1 = 5,
  UNRECOGNIZED = -1,
}

export function candleIntervalFromJSON(object: any): CandleInterval {
  switch (object) {
    case 0:
    case "CANDLE_INTERVAL_UNSPECIFIED":
      return CandleInterval.CANDLE_INTERVAL_UNSPECIFIED;
    case 1:
    case "MIN_1":
      return CandleInterval.MIN_1;
    case 2:
    case "MIN_5":
      return CandleInterval.MIN_5;
    case 3:
    case "DAY_1":
      return CandleInterval.DAY_1;
    case 4:
    case "WEEK_1":
      return CandleInterval.WEEK_1;
    case 5:
    case "MONTH_1":
      return CandleInterval.MONTH_1;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CandleInterval.UNRECOGNIZED;
  }
}

export function candleIntervalToJSON(object: CandleInterval): string {
  switch (object) {
    case CandleInterval.CANDLE_INTERVAL_UNSPECIFIED:
      return "CANDLE_INTERVAL_UNSPECIFIED";
    case CandleInterval.MIN_1:
      return "MIN_1";
    case CandleInterval.MIN_5:
      return "MIN_5";
    case CandleInterval.DAY_1:
      return "DAY_1";
    case CandleInterval.WEEK_1:
      return "WEEK_1";
    case CandleInterval.MONTH_1:
      return "MONTH_1";
    case CandleInterval.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Candle {
  code: string;
  interval: CandleInterval;
  openTime?: Date | undefined;
  open: string;
  high: string;
  low: string;
  close: string;
  volume: string;
  closed: boolean;
}

export interface GetCandlesRequest {
  code: string;
  interval: CandleInterval;
  limit: number;
  to?: Date | undefined;
}

export interface GetCandlesResponse {
  candles: Candle[];
}

export interface BackfillCandlesRequest {
  code: string;
  interval: CandleInterval;
  count: number;
}

export interface BackfillCandlesResponse {
  upserted: number;
}

function createBaseCandle(): Candle {
  return {
    code: "",
    interval: 0,
    openTime: undefined,
    open: "0",
    high: "0",
    low: "0",
    close: "0",
    volume: "0",
    closed: false,
  };
}

export const Candle: MessageFns<Candle> = {
  encode(message: Candle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.interval !== 0) {
      writer.uint32(16).int32(message.interval);
    }
    if (message.openTime !== undefined) {
      Timestamp.encode(toTimestamp(message.openTime), writer.uint32(26).fork()).join();
    }
    if (message.open !== "0") {
      writer.uint32(32).int64(message.open);
    }
    if (message.high !== "0") {
      writer.uint32(40).int64(message.high);
    }
    if (message.low !== "0") {
      writer.uint32(48).int64(message.low);
    }
    if (message.close !== "0") {
      writer.uint32(56).int64(message.close);
    }
    if (message.volume !== "0") {
      writer.uint32(64).int64(message.volume);
    }
    if (message.closed !== false) {
      writer.uint32(72).bool(message.closed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Candle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCandle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.interval = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.openTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.open = reader.int64().toString();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.high = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.low = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.close = reader.int64().toString();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.volume = reader.int64().toString();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.closed = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Candle {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      interval: isSet(object.interval) ? candleIntervalFromJSON(object.interval) : 0,
      openTime: isSet(object.openTime)
        ? fromJsonTimestamp(object.openTime)
        : isSet(object.open_time)
        ? fromJsonTimestamp(object.open_time)
        : undefined,
      open: isSet(object.open) ? globalThis.String(object.open) : "0",
      high: isSet(object.high) ? globalThis.String(object.high) : "0",
      low: isSet(object.low) ? globalThis.String(object.low) : "0",
      close: isSet(object.close) ? globalThis.String(object.close) : "0",
      volume: isSet(object.volume) ? globalThis.String(object.volume) : "0",
      closed: isSet(object.closed) ? globalThis.Boolean(object.closed) : false,
    };
  },

  toJSON(message: Candle): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.interval !== 0) {
      obj.interval = candleIntervalToJSON(message.interval);
    }
    if (message.openTime !== undefined) {
      obj.openTime = message.openTime.toISOString();
    }
    if (message.open !== "0") {
      obj.open = message.open;
    }
    if (message.high !== "0") {
      obj.high = message.high;
    }
    if (message.low !== "0") {
      obj.low = message.low;
    }
    if (message.close !== "0") {
      obj.close = message.close;
    }
    if (message.volume !== "0") {
      obj.volume = message.volume;
    }
    if (message.closed !== false) {
      obj.closed = message.closed;
    }
    return obj;
  },

  create(base?: DeepPartial<Candle>): Candle {
    return Candle.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Candle>): Candle {
    const message = createBaseCandle();
    message.code = object.code ?? "";
    message.interval = object.interval ?? 0;
    message.openTime = object.openTime ?? undefined;
    message.open = object.open ?? "0";
    message.high = object.high ?? "0";
    message.low = object.low ?? "0";
    message.close = object.close ?? "0";
    message.volume = object.volume ?? "0";
    message.closed = object.closed ?? false;
    return message;
  },
};

function createBaseGetCandlesRequest(): GetCandlesRequest {
  return { code: "", interval: 0, limit: 0, to: undefined };
}

export const GetCandlesRequest: MessageFns<GetCandlesRequest> = {
  encode(message: GetCandlesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.interval !== 0) {
      writer.uint32(16).int32(message.interval);
    }
    if (message.limit !== 0) {
      writer.uint32(24).int32(message.limit);
    }
    if (message.to !== undefined) {
      Timestamp.encode(toTimestamp(message.to), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetCandlesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetCandlesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.interval = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.to = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetCandlesRequest {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      interval: isSet(object.interval) ? candleIntervalFromJSON(object.interval) : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      to: isSet(object.to) ? fromJsonTimestamp(object.to) : undefined,
    };
  },

  toJSON(message: GetCandlesRequest): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.interval !== 0) {
      obj.interval = candleIntervalToJSON(message.interval);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.to !== undefined) {
      obj.to = message.to.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<GetCandlesRequest>): GetCandlesRequest {
    return GetCandlesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetCandlesRequest>): GetCandlesRequest {
    const message = createBaseGetCandlesRequest();
    message.code = object.code ?? "";
    message.interval = object.interval ?? 0;
    message.limit = object.limit ?? 0;
    message.to = object.to ?? undefined;
    return message;
  },
};

function createBaseGetCandlesResponse(): GetCandlesResponse {
  return { candles: [] };
}

export const GetCandlesResponse: MessageFns<GetCandlesResponse> = {
  encode(message: GetCandlesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.candles) {
      Candle.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetCandlesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetCandlesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.candles.push(Candle.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetCandlesResponse {
    return {
      candles: globalThis.Array.isArray(object?.candles) ? object.candles.map((e: any) => Candle.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetCandlesResponse): unknown {
    const obj: any = {};
    if (message.candles?.length) {
      obj.candles = message.candles.map((e) => Candle.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetCandlesResponse>): GetCandlesResponse {
    return GetCandlesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetCandlesResponse>): GetCandlesResponse {
    const message = createBaseGetCandlesResponse();
    message.candles = object.candles?.map((e) => Candle.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBackfillCandlesRequest(): BackfillCandlesRequest {
  return { code: "", interval: 0, count: 0 };
}

export const BackfillCandlesRequest: MessageFns<BackfillCandlesRequest> = {
  encode(message: BackfillCandlesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.interval !== 0) {
      writer.uint32(16).int32(message.interval);
    }
    if (message.count !== 0) {
      writer.uint32(24).int32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BackfillCandlesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBackfillCandlesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.interval = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BackfillCandlesRequest {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      interval: isSet(object.interval) ? candleIntervalFromJSON(object.interval) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: BackfillCandlesRequest): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.interval !== 0) {
      obj.interval = candleIntervalToJSON(message.interval);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create(base?: DeepPartial<BackfillCandlesRequest>): BackfillCandlesRequest {
    return BackfillCandlesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BackfillCandlesRequest>): BackfillCandlesRequest {
    const message = createBaseBackfillCandlesRequest();
    message.code = object.code ?? "";
    message.interval = object.interval ?? 0;
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseBackfillCandlesResponse(): BackfillCandlesResponse {
  return { upserted: 0 };
}

export const BackfillCandlesResponse: MessageFns<BackfillCandlesResponse> = {
  encode(message: BackfillCandlesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upserted !== 0) {
      writer.uint32(8).int32(message.upserted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BackfillCandlesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBackfillCandlesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.upserted = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BackfillCandlesResponse {
    return { upserted: isSet(object.upserted) ? globalThis.Number(object.upserted) : 0 };
  },

  toJSON(message: BackfillCandlesResponse): unknown {
    const obj: any = {};
    if (message.upserted !== 0) {
      obj.upserted = Math.round(message.upserted);
    }
    return obj;
  },

  create(base?: DeepPartial<BackfillCandlesResponse>): BackfillCandlesResponse {
    return BackfillCandlesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BackfillCandlesResponse>): BackfillCandlesResponse {
    const message = createBaseBackfillCandlesResponse();
    message.upserted = object.upserted ?? 0;
    return message;
  },
};

export type ChartServiceDefinition = typeof ChartServiceDefinition;
export const ChartServiceDefinition = {
  name: "ChartService",
  fullName: "candle.stock.v1.ChartService",
  methods: {
    getCandles: {
      name: "GetCandles",
      requestType: GetCandlesRequest as typeof GetCandlesRequest,
      requestStream: false,
      responseType: GetCandlesResponse as typeof GetCandlesResponse,
      responseStream: false,
      options: {},
    },
    backfillCandles: {
      name: "BackfillCandles",
      requestType: BackfillCandlesRequest as typeof BackfillCandlesRequest,
      requestStream: false,
      responseType: BackfillCandlesResponse as typeof BackfillCandlesResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface ChartServiceImplementation<CallContextExt = {}> {
  getCandles(
    request: GetCandlesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetCandlesResponse>>;
  backfillCandles(
    request: BackfillCandlesRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<BackfillCandlesResponse>>;
}

export interface ChartServiceClient<CallOptionsExt = {}> {
  getCandles(
    request: DeepPartial<GetCandlesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetCandlesResponse>;
  backfillCandles(
    request: DeepPartial<BackfillCandlesRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<BackfillCandlesResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
