// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: candle/stock/v1/stock.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { Audit } from "../../common/v1/common";

export const protobufPackage = "candle.stock.v1";

export enum MarketType {
  MARKET_TYPE_UNSPECIFIED = 0,
  KOSPI = 1,
  KOSDAQ = 2,
  UNRECOGNIZED = -1,
}

export function marketTypeFromJSON(object: any): MarketType {
  switch (object) {
    case 0:
    case "MARKET_TYPE_UNSPECIFIED":
      return MarketType.MARKET_TYPE_UNSPECIFIED;
    case 1:
    case "KOSPI":
      return MarketType.KOSPI;
    case 2:
    case "KOSDAQ":
      return MarketType.KOSDAQ;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MarketType.UNRECOGNIZED;
  }
}

export function marketTypeToJSON(object: MarketType): string {
  switch (object) {
    case MarketType.MARKET_TYPE_UNSPECIFIED:
      return "MARKET_TYPE_UNSPECIFIED";
    case MarketType.KOSPI:
      return "KOSPI";
    case MarketType.KOSDAQ:
      return "KOSDAQ";
    case MarketType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ListingStatus {
  LISTING_STATUS_UNSPECIFIED = 0,
  LISTED = 1,
  DELISTED = 2,
  SUSPENDED = 3,
  UNRECOGNIZED = -1,
}

export function listingStatusFromJSON(object: any): ListingStatus {
  switch (object) {
    case 0:
    case "LISTING_STATUS_UNSPECIFIED":
      return ListingStatus.LISTING_STATUS_UNSPECIFIED;
    case 1:
    case "LISTED":
      return ListingStatus.LISTED;
    case 2:
    case "DELISTED":
      return ListingStatus.DELISTED;
    case 3:
    case "SUSPENDED":
      return ListingStatus.SUSPENDED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ListingStatus.UNRECOGNIZED;
  }
}

export function listingStatusToJSON(object: ListingStatus): string {
  switch (object) {
    case ListingStatus.LISTING_STATUS_UNSPECIFIED:
      return "LISTING_STATUS_UNSPECIFIED";
    case ListingStatus.LISTED:
      return "LISTED";
    case ListingStatus.DELISTED:
      return "DELISTED";
    case ListingStatus.SUSPENDED:
      return "SUSPENDED";
    case ListingStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum StockSort {
  /** STOCK_SORT_UNSPECIFIED - 기본: CODE_ASC */
  STOCK_SORT_UNSPECIFIED = 0,
  CODE_ASC = 1,
  NAME_ASC = 2,
  MARKET_CAP_DESC = 3,
  UNRECOGNIZED = -1,
}

export function stockSortFromJSON(object: any): StockSort {
  switch (object) {
    case 0:
    case "STOCK_SORT_UNSPECIFIED":
      return StockSort.STOCK_SORT_UNSPECIFIED;
    case 1:
    case "CODE_ASC":
      return StockSort.CODE_ASC;
    case 2:
    case "NAME_ASC":
      return StockSort.NAME_ASC;
    case 3:
    case "MARKET_CAP_DESC":
      return StockSort.MARKET_CAP_DESC;
    case -1:
    case "UNRECOGNIZED":
    default:
      return StockSort.UNRECOGNIZED;
  }
}

export function stockSortToJSON(object: StockSort): string {
  switch (object) {
    case StockSort.STOCK_SORT_UNSPECIFIED:
      return "STOCK_SORT_UNSPECIFIED";
    case StockSort.CODE_ASC:
      return "CODE_ASC";
    case StockSort.NAME_ASC:
      return "NAME_ASC";
    case StockSort.MARKET_CAP_DESC:
      return "MARKET_CAP_DESC";
    case StockSort.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** 응답이 DB에서 왔는지, 키움 fallback으로 채워졌는지 구분. */
export enum DataSource {
  DATA_SOURCE_UNSPECIFIED = 0,
  DB = 1,
  KIWOOM = 2,
  UNRECOGNIZED = -1,
}

export function dataSourceFromJSON(object: any): DataSource {
  switch (object) {
    case 0:
    case "DATA_SOURCE_UNSPECIFIED":
      return DataSource.DATA_SOURCE_UNSPECIFIED;
    case 1:
    case "DB":
      return DataSource.DB;
    case 2:
    case "KIWOOM":
      return DataSource.KIWOOM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DataSource.UNRECOGNIZED;
  }
}

export function dataSourceToJSON(object: DataSource): string {
  switch (object) {
    case DataSource.DATA_SOURCE_UNSPECIFIED:
      return "DATA_SOURCE_UNSPECIFIED";
    case DataSource.DB:
      return "DB";
    case DataSource.KIWOOM:
      return "KIWOOM";
    case DataSource.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Stock {
  /** 종목코드 (6자리) */
  code: string;
  name: string;
  market: MarketType;
  /** 업종 */
  sector: string;
  /** 시가총액 (배치 갱신, 없으면 0) */
  marketCap: string;
  sharesOutstanding: string;
  status: ListingStatus;
  /** 상장일 */
  listedAt?: Date | undefined;
  audit?: Audit | undefined;
}

/** 재무지표는 소수 정밀도 보존을 위해 Decimal-string(common.Decimal 규약)으로 전달. */
export interface StockFinancials {
  /** 매출액 */
  revenue: string;
  /** 영업이익 */
  operatingProfit: string;
  /** 순이익 */
  netIncome: string;
  per: string;
  pbr: string;
  /** % */
  roe: string;
  /** 예: "2025Q1" */
  fiscalPeriod: string;
}

export interface StockDetail {
  stock?: Stock | undefined;
  financials?: StockFinancials | undefined;
  description: string;
}

/**
 * ── 조회 (BFF가 호출) ─────────────────────────────────────────────
 * 목록/검색: 페이지번호 + total (DB 전용 — 배치/스캐너가 선적재).
 *
 * CONVENTIONS §13 예외: 공통 page_token 커서 대신 page/size(offset)을 쓴다.
 *   사유 — 종목 카탈로그는 ~4천건·저빈도 변경이라 §13 자체 기준("대량·고빈도만 커서")에서
 *   offset 이 허용되며, 프론트 종목표는 페이지번호+total UX 가 필요하다.
 *   size 는 서버에서 최대 100 으로 강제하고, 정렬은 stock_code 보조키로 안정화한다.
 */
export interface SearchStocksRequest {
  /** 코드/이름 부분검색 (선택) */
  query: string;
  /** 필터 (선택) */
  market: MarketType;
  /** 필터 (선택) */
  sector: string;
  /** 기본 LISTED */
  status: ListingStatus;
  sort: StockSort;
  /** 0-based */
  page: number;
  /** 기본 20, 최대 100 */
  size: number;
}

export interface SearchStocksResponse {
  stocks: Stock[];
  totalElements: string;
  totalPages: number;
  page: number;
  size: number;
}

/** 상세: DB miss/stale 이고 allow_fallback 이면 키움 조회 후 적재하여 반환. */
export interface GetStockRequest {
  code: string;
  allowFallback: boolean;
}

export interface GetStockResponse {
  stock?: StockDetail | undefined;
  source: DataSource;
}

export interface BatchGetStocksRequest {
  codes: string[];
}

export interface BatchGetStocksResponse {
  stocks: Stock[];
}

/**
 * ── 적재 (배치/관리자가 호출) ─────────────────────────────────────
 * 키움에서 시장별 전체 종목을 pull 하여 벌크 upsert.
 */
export interface SyncStocksRequest {
  /** UNSPECIFIED 면 전체 시장 */
  market: MarketType;
}

export interface SyncStocksResponse {
  upserted: number;
  total: number;
}

function createBaseStock(): Stock {
  return {
    code: "",
    name: "",
    market: 0,
    sector: "",
    marketCap: "0",
    sharesOutstanding: "0",
    status: 0,
    listedAt: undefined,
    audit: undefined,
  };
}

export const Stock: MessageFns<Stock> = {
  encode(message: Stock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.market !== 0) {
      writer.uint32(24).int32(message.market);
    }
    if (message.sector !== "") {
      writer.uint32(34).string(message.sector);
    }
    if (message.marketCap !== "0") {
      writer.uint32(40).int64(message.marketCap);
    }
    if (message.sharesOutstanding !== "0") {
      writer.uint32(48).int64(message.sharesOutstanding);
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    if (message.listedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.listedAt), writer.uint32(66).fork()).join();
    }
    if (message.audit !== undefined) {
      Audit.encode(message.audit, writer.uint32(162).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Stock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStock();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.market = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sector = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.marketCap = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.sharesOutstanding = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.listedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.audit = Audit.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Stock {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      market: isSet(object.market) ? marketTypeFromJSON(object.market) : 0,
      sector: isSet(object.sector) ? globalThis.String(object.sector) : "",
      marketCap: isSet(object.marketCap)
        ? globalThis.String(object.marketCap)
        : isSet(object.market_cap)
        ? globalThis.String(object.market_cap)
        : "0",
      sharesOutstanding: isSet(object.sharesOutstanding)
        ? globalThis.String(object.sharesOutstanding)
        : isSet(object.shares_outstanding)
        ? globalThis.String(object.shares_outstanding)
        : "0",
      status: isSet(object.status) ? listingStatusFromJSON(object.status) : 0,
      listedAt: isSet(object.listedAt)
        ? fromJsonTimestamp(object.listedAt)
        : isSet(object.listed_at)
        ? fromJsonTimestamp(object.listed_at)
        : undefined,
      audit: isSet(object.audit) ? Audit.fromJSON(object.audit) : undefined,
    };
  },

  toJSON(message: Stock): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.market !== 0) {
      obj.market = marketTypeToJSON(message.market);
    }
    if (message.sector !== "") {
      obj.sector = message.sector;
    }
    if (message.marketCap !== "0") {
      obj.marketCap = message.marketCap;
    }
    if (message.sharesOutstanding !== "0") {
      obj.sharesOutstanding = message.sharesOutstanding;
    }
    if (message.status !== 0) {
      obj.status = listingStatusToJSON(message.status);
    }
    if (message.listedAt !== undefined) {
      obj.listedAt = message.listedAt.toISOString();
    }
    if (message.audit !== undefined) {
      obj.audit = Audit.toJSON(message.audit);
    }
    return obj;
  },

  create(base?: DeepPartial<Stock>): Stock {
    return Stock.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Stock>): Stock {
    const message = createBaseStock();
    message.code = object.code ?? "";
    message.name = object.name ?? "";
    message.market = object.market ?? 0;
    message.sector = object.sector ?? "";
    message.marketCap = object.marketCap ?? "0";
    message.sharesOutstanding = object.sharesOutstanding ?? "0";
    message.status = object.status ?? 0;
    message.listedAt = object.listedAt ?? undefined;
    message.audit = (object.audit !== undefined && object.audit !== null) ? Audit.fromPartial(object.audit) : undefined;
    return message;
  },
};

function createBaseStockFinancials(): StockFinancials {
  return { revenue: "0", operatingProfit: "0", netIncome: "0", per: "", pbr: "", roe: "", fiscalPeriod: "" };
}

export const StockFinancials: MessageFns<StockFinancials> = {
  encode(message: StockFinancials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revenue !== "0") {
      writer.uint32(8).int64(message.revenue);
    }
    if (message.operatingProfit !== "0") {
      writer.uint32(16).int64(message.operatingProfit);
    }
    if (message.netIncome !== "0") {
      writer.uint32(24).int64(message.netIncome);
    }
    if (message.per !== "") {
      writer.uint32(34).string(message.per);
    }
    if (message.pbr !== "") {
      writer.uint32(42).string(message.pbr);
    }
    if (message.roe !== "") {
      writer.uint32(50).string(message.roe);
    }
    if (message.fiscalPeriod !== "") {
      writer.uint32(58).string(message.fiscalPeriod);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StockFinancials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStockFinancials();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.revenue = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.operatingProfit = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.netIncome = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.per = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.pbr = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.roe = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.fiscalPeriod = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StockFinancials {
    return {
      revenue: isSet(object.revenue) ? globalThis.String(object.revenue) : "0",
      operatingProfit: isSet(object.operatingProfit)
        ? globalThis.String(object.operatingProfit)
        : isSet(object.operating_profit)
        ? globalThis.String(object.operating_profit)
        : "0",
      netIncome: isSet(object.netIncome)
        ? globalThis.String(object.netIncome)
        : isSet(object.net_income)
        ? globalThis.String(object.net_income)
        : "0",
      per: isSet(object.per) ? globalThis.String(object.per) : "",
      pbr: isSet(object.pbr) ? globalThis.String(object.pbr) : "",
      roe: isSet(object.roe) ? globalThis.String(object.roe) : "",
      fiscalPeriod: isSet(object.fiscalPeriod)
        ? globalThis.String(object.fiscalPeriod)
        : isSet(object.fiscal_period)
        ? globalThis.String(object.fiscal_period)
        : "",
    };
  },

  toJSON(message: StockFinancials): unknown {
    const obj: any = {};
    if (message.revenue !== "0") {
      obj.revenue = message.revenue;
    }
    if (message.operatingProfit !== "0") {
      obj.operatingProfit = message.operatingProfit;
    }
    if (message.netIncome !== "0") {
      obj.netIncome = message.netIncome;
    }
    if (message.per !== "") {
      obj.per = message.per;
    }
    if (message.pbr !== "") {
      obj.pbr = message.pbr;
    }
    if (message.roe !== "") {
      obj.roe = message.roe;
    }
    if (message.fiscalPeriod !== "") {
      obj.fiscalPeriod = message.fiscalPeriod;
    }
    return obj;
  },

  create(base?: DeepPartial<StockFinancials>): StockFinancials {
    return StockFinancials.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StockFinancials>): StockFinancials {
    const message = createBaseStockFinancials();
    message.revenue = object.revenue ?? "0";
    message.operatingProfit = object.operatingProfit ?? "0";
    message.netIncome = object.netIncome ?? "0";
    message.per = object.per ?? "";
    message.pbr = object.pbr ?? "";
    message.roe = object.roe ?? "";
    message.fiscalPeriod = object.fiscalPeriod ?? "";
    return message;
  },
};

function createBaseStockDetail(): StockDetail {
  return { stock: undefined, financials: undefined, description: "" };
}

export const StockDetail: MessageFns<StockDetail> = {
  encode(message: StockDetail, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stock !== undefined) {
      Stock.encode(message.stock, writer.uint32(10).fork()).join();
    }
    if (message.financials !== undefined) {
      StockFinancials.encode(message.financials, writer.uint32(18).fork()).join();
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StockDetail {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStockDetail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stock = Stock.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.financials = StockFinancials.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StockDetail {
    return {
      stock: isSet(object.stock) ? Stock.fromJSON(object.stock) : undefined,
      financials: isSet(object.financials) ? StockFinancials.fromJSON(object.financials) : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: StockDetail): unknown {
    const obj: any = {};
    if (message.stock !== undefined) {
      obj.stock = Stock.toJSON(message.stock);
    }
    if (message.financials !== undefined) {
      obj.financials = StockFinancials.toJSON(message.financials);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create(base?: DeepPartial<StockDetail>): StockDetail {
    return StockDetail.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StockDetail>): StockDetail {
    const message = createBaseStockDetail();
    message.stock = (object.stock !== undefined && object.stock !== null) ? Stock.fromPartial(object.stock) : undefined;
    message.financials = (object.financials !== undefined && object.financials !== null)
      ? StockFinancials.fromPartial(object.financials)
      : undefined;
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseSearchStocksRequest(): SearchStocksRequest {
  return { query: "", market: 0, sector: "", status: 0, sort: 0, page: 0, size: 0 };
}

export const SearchStocksRequest: MessageFns<SearchStocksRequest> = {
  encode(message: SearchStocksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.market !== 0) {
      writer.uint32(16).int32(message.market);
    }
    if (message.sector !== "") {
      writer.uint32(26).string(message.sector);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.sort !== 0) {
      writer.uint32(40).int32(message.sort);
    }
    if (message.page !== 0) {
      writer.uint32(48).int32(message.page);
    }
    if (message.size !== 0) {
      writer.uint32(56).int32(message.size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchStocksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchStocksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.market = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sector = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.sort = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.size = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchStocksRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      market: isSet(object.market) ? marketTypeFromJSON(object.market) : 0,
      sector: isSet(object.sector) ? globalThis.String(object.sector) : "",
      status: isSet(object.status) ? listingStatusFromJSON(object.status) : 0,
      sort: isSet(object.sort) ? stockSortFromJSON(object.sort) : 0,
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
    };
  },

  toJSON(message: SearchStocksRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.market !== 0) {
      obj.market = marketTypeToJSON(message.market);
    }
    if (message.sector !== "") {
      obj.sector = message.sector;
    }
    if (message.status !== 0) {
      obj.status = listingStatusToJSON(message.status);
    }
    if (message.sort !== 0) {
      obj.sort = stockSortToJSON(message.sort);
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchStocksRequest>): SearchStocksRequest {
    return SearchStocksRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchStocksRequest>): SearchStocksRequest {
    const message = createBaseSearchStocksRequest();
    message.query = object.query ?? "";
    message.market = object.market ?? 0;
    message.sector = object.sector ?? "";
    message.status = object.status ?? 0;
    message.sort = object.sort ?? 0;
    message.page = object.page ?? 0;
    message.size = object.size ?? 0;
    return message;
  },
};

function createBaseSearchStocksResponse(): SearchStocksResponse {
  return { stocks: [], totalElements: "0", totalPages: 0, page: 0, size: 0 };
}

export const SearchStocksResponse: MessageFns<SearchStocksResponse> = {
  encode(message: SearchStocksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.stocks) {
      Stock.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalElements !== "0") {
      writer.uint32(16).int64(message.totalElements);
    }
    if (message.totalPages !== 0) {
      writer.uint32(24).int32(message.totalPages);
    }
    if (message.page !== 0) {
      writer.uint32(32).int32(message.page);
    }
    if (message.size !== 0) {
      writer.uint32(40).int32(message.size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchStocksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchStocksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stocks.push(Stock.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalElements = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalPages = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.size = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchStocksResponse {
    return {
      stocks: globalThis.Array.isArray(object?.stocks) ? object.stocks.map((e: any) => Stock.fromJSON(e)) : [],
      totalElements: isSet(object.totalElements)
        ? globalThis.String(object.totalElements)
        : isSet(object.total_elements)
        ? globalThis.String(object.total_elements)
        : "0",
      totalPages: isSet(object.totalPages)
        ? globalThis.Number(object.totalPages)
        : isSet(object.total_pages)
        ? globalThis.Number(object.total_pages)
        : 0,
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
    };
  },

  toJSON(message: SearchStocksResponse): unknown {
    const obj: any = {};
    if (message.stocks?.length) {
      obj.stocks = message.stocks.map((e) => Stock.toJSON(e));
    }
    if (message.totalElements !== "0") {
      obj.totalElements = message.totalElements;
    }
    if (message.totalPages !== 0) {
      obj.totalPages = Math.round(message.totalPages);
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    return obj;
  },

  create(base?: DeepPartial<SearchStocksResponse>): SearchStocksResponse {
    return SearchStocksResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SearchStocksResponse>): SearchStocksResponse {
    const message = createBaseSearchStocksResponse();
    message.stocks = object.stocks?.map((e) => Stock.fromPartial(e)) || [];
    message.totalElements = object.totalElements ?? "0";
    message.totalPages = object.totalPages ?? 0;
    message.page = object.page ?? 0;
    message.size = object.size ?? 0;
    return message;
  },
};

function createBaseGetStockRequest(): GetStockRequest {
  return { code: "", allowFallback: false };
}

export const GetStockRequest: MessageFns<GetStockRequest> = {
  encode(message: GetStockRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.allowFallback !== false) {
      writer.uint32(16).bool(message.allowFallback);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStockRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStockRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.allowFallback = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStockRequest {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      allowFallback: isSet(object.allowFallback)
        ? globalThis.Boolean(object.allowFallback)
        : isSet(object.allow_fallback)
        ? globalThis.Boolean(object.allow_fallback)
        : false,
    };
  },

  toJSON(message: GetStockRequest): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.allowFallback !== false) {
      obj.allowFallback = message.allowFallback;
    }
    return obj;
  },

  create(base?: DeepPartial<GetStockRequest>): GetStockRequest {
    return GetStockRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetStockRequest>): GetStockRequest {
    const message = createBaseGetStockRequest();
    message.code = object.code ?? "";
    message.allowFallback = object.allowFallback ?? false;
    return message;
  },
};

function createBaseGetStockResponse(): GetStockResponse {
  return { stock: undefined, source: 0 };
}

export const GetStockResponse: MessageFns<GetStockResponse> = {
  encode(message: GetStockResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stock !== undefined) {
      StockDetail.encode(message.stock, writer.uint32(10).fork()).join();
    }
    if (message.source !== 0) {
      writer.uint32(16).int32(message.source);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStockResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStockResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stock = StockDetail.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStockResponse {
    return {
      stock: isSet(object.stock) ? StockDetail.fromJSON(object.stock) : undefined,
      source: isSet(object.source) ? dataSourceFromJSON(object.source) : 0,
    };
  },

  toJSON(message: GetStockResponse): unknown {
    const obj: any = {};
    if (message.stock !== undefined) {
      obj.stock = StockDetail.toJSON(message.stock);
    }
    if (message.source !== 0) {
      obj.source = dataSourceToJSON(message.source);
    }
    return obj;
  },

  create(base?: DeepPartial<GetStockResponse>): GetStockResponse {
    return GetStockResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetStockResponse>): GetStockResponse {
    const message = createBaseGetStockResponse();
    message.stock = (object.stock !== undefined && object.stock !== null)
      ? StockDetail.fromPartial(object.stock)
      : undefined;
    message.source = object.source ?? 0;
    return message;
  },
};

function createBaseBatchGetStocksRequest(): BatchGetStocksRequest {
  return { codes: [] };
}

export const BatchGetStocksRequest: MessageFns<BatchGetStocksRequest> = {
  encode(message: BatchGetStocksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.codes) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchGetStocksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchGetStocksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.codes.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchGetStocksRequest {
    return { codes: globalThis.Array.isArray(object?.codes) ? object.codes.map((e: any) => globalThis.String(e)) : [] };
  },

  toJSON(message: BatchGetStocksRequest): unknown {
    const obj: any = {};
    if (message.codes?.length) {
      obj.codes = message.codes;
    }
    return obj;
  },

  create(base?: DeepPartial<BatchGetStocksRequest>): BatchGetStocksRequest {
    return BatchGetStocksRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BatchGetStocksRequest>): BatchGetStocksRequest {
    const message = createBaseBatchGetStocksRequest();
    message.codes = object.codes?.map((e) => e) || [];
    return message;
  },
};

function createBaseBatchGetStocksResponse(): BatchGetStocksResponse {
  return { stocks: [] };
}

export const BatchGetStocksResponse: MessageFns<BatchGetStocksResponse> = {
  encode(message: BatchGetStocksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.stocks) {
      Stock.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchGetStocksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchGetStocksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stocks.push(Stock.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchGetStocksResponse {
    return { stocks: globalThis.Array.isArray(object?.stocks) ? object.stocks.map((e: any) => Stock.fromJSON(e)) : [] };
  },

  toJSON(message: BatchGetStocksResponse): unknown {
    const obj: any = {};
    if (message.stocks?.length) {
      obj.stocks = message.stocks.map((e) => Stock.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<BatchGetStocksResponse>): BatchGetStocksResponse {
    return BatchGetStocksResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BatchGetStocksResponse>): BatchGetStocksResponse {
    const message = createBaseBatchGetStocksResponse();
    message.stocks = object.stocks?.map((e) => Stock.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSyncStocksRequest(): SyncStocksRequest {
  return { market: 0 };
}

export const SyncStocksRequest: MessageFns<SyncStocksRequest> = {
  encode(message: SyncStocksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.market !== 0) {
      writer.uint32(8).int32(message.market);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncStocksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncStocksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.market = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncStocksRequest {
    return { market: isSet(object.market) ? marketTypeFromJSON(object.market) : 0 };
  },

  toJSON(message: SyncStocksRequest): unknown {
    const obj: any = {};
    if (message.market !== 0) {
      obj.market = marketTypeToJSON(message.market);
    }
    return obj;
  },

  create(base?: DeepPartial<SyncStocksRequest>): SyncStocksRequest {
    return SyncStocksRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SyncStocksRequest>): SyncStocksRequest {
    const message = createBaseSyncStocksRequest();
    message.market = object.market ?? 0;
    return message;
  },
};

function createBaseSyncStocksResponse(): SyncStocksResponse {
  return { upserted: 0, total: 0 };
}

export const SyncStocksResponse: MessageFns<SyncStocksResponse> = {
  encode(message: SyncStocksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upserted !== 0) {
      writer.uint32(8).int32(message.upserted);
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncStocksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncStocksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.upserted = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncStocksResponse {
    return {
      upserted: isSet(object.upserted) ? globalThis.Number(object.upserted) : 0,
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: SyncStocksResponse): unknown {
    const obj: any = {};
    if (message.upserted !== 0) {
      obj.upserted = Math.round(message.upserted);
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create(base?: DeepPartial<SyncStocksResponse>): SyncStocksResponse {
    return SyncStocksResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SyncStocksResponse>): SyncStocksResponse {
    const message = createBaseSyncStocksResponse();
    message.upserted = object.upserted ?? 0;
    message.total = object.total ?? 0;
    return message;
  },
};

export type StockServiceDefinition = typeof StockServiceDefinition;
export const StockServiceDefinition = {
  name: "StockService",
  fullName: "candle.stock.v1.StockService",
  methods: {
    searchStocks: {
      name: "SearchStocks",
      requestType: SearchStocksRequest as typeof SearchStocksRequest,
      requestStream: false,
      responseType: SearchStocksResponse as typeof SearchStocksResponse,
      responseStream: false,
      options: {},
    },
    getStock: {
      name: "GetStock",
      requestType: GetStockRequest as typeof GetStockRequest,
      requestStream: false,
      responseType: GetStockResponse as typeof GetStockResponse,
      responseStream: false,
      options: {},
    },
    batchGetStocks: {
      name: "BatchGetStocks",
      requestType: BatchGetStocksRequest as typeof BatchGetStocksRequest,
      requestStream: false,
      responseType: BatchGetStocksResponse as typeof BatchGetStocksResponse,
      responseStream: false,
      options: {},
    },
    syncStocks: {
      name: "SyncStocks",
      requestType: SyncStocksRequest as typeof SyncStocksRequest,
      requestStream: false,
      responseType: SyncStocksResponse as typeof SyncStocksResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface StockServiceImplementation<CallContextExt = {}> {
  searchStocks(
    request: SearchStocksRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<SearchStocksResponse>>;
  getStock(request: GetStockRequest, context: CallContext & CallContextExt): Promise<DeepPartial<GetStockResponse>>;
  batchGetStocks(
    request: BatchGetStocksRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<BatchGetStocksResponse>>;
  syncStocks(
    request: SyncStocksRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<SyncStocksResponse>>;
}

export interface StockServiceClient<CallOptionsExt = {}> {
  searchStocks(
    request: DeepPartial<SearchStocksRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<SearchStocksResponse>;
  getStock(request: DeepPartial<GetStockRequest>, options?: CallOptions & CallOptionsExt): Promise<GetStockResponse>;
  batchGetStocks(
    request: DeepPartial<BatchGetStocksRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<BatchGetStocksResponse>;
  syncStocks(
    request: DeepPartial<SyncStocksRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<SyncStocksResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
